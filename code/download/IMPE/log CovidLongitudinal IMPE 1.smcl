{smcl}
{com}{sf}{ul off}{txt}{.-}
      name:  {res}<unnamed>
       {txt}log:  {res}/Users/farshadpourmalek/Downloads/CovidLongitudinal-main/code/download/IMPE/log CovidLongitudinal IMPE 1.smcl
  {txt}log type:  {res}smcl
 {txt}opened on:  {res}27 Aug 2023, 16:07:30
{txt}
{com}. 
. ***************************************************************************
. * This is "do CovidLongitudinal IMPE 1.do"
. 
. * Project: Longitudinal assessment of COVID-19 models                                                                         
. 
. * Objective: Download estimates files for IMPE model, part 1 of 3                                                                   
. ***************************************************************************
. 
. 
. * download estimates files - part 1 of 3 - 2020 (from 2020, 2021, 2022)
. * input data files: csv files on web site
. * output data files: "IMPE `update'.dta" 
. 
. 
. 
. 
. 
. /*
> 
> History of existence of | sun.security.validator | error while downloading IMPE estimates files from web page https://mrcdata.dide.ic.ac.uk/global-lmic-reports/
> 
> (1) Before October 2022, there was no such | sun.security.validator | error
> 
> (2) In October 2022, the | sun.security.validator | error occurred. There is a BYPASS REMEDY for that explained below. 
> 
> (3) On November 4, 2022, the | sun.security.validator | error does not occur. Therefore, the BYPASS REMEDY is turned off below in the code. 
> 
> (4) In case the | sun.security.validator | error occurs again, reverting back to the BYPASS REMEDY should be able to get rid of the consequences of the error. 
> 
> */
. 
. 
. 
. 
. 
. 
. /*
> 
>  *** BYPASS REMEDY for | sun.security.validator | error while downloading IMPE estimates files from web page 
>  *** from web page https://mrcdata.dide.ic.ac.uk/global-lmic-reports/
>  
> The Stata command -copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/202?-??-??_v?.csv.zip 202?-??-??_v?.csv.zip- 
> 
> worked without error before October 2022, but as of October 2022, it returns the following error:
> 
> 
> sun.security.validator.ValidatorException: PKIX path building failed: sun.security.provider.certpath.SunCertPathBuilderEx
> > ception: unable to find valid certification path to requested target
> r(5100);
> 
> *
> 
> * -set sslrelax on- is mentioned by Stata here:
> https://www.statalist.org/forums/forum/general-stata-discussion/general/1625838-pkix-path-building-failed-error-message
> 
> but does not work with Stata SE 14.2 on Mac OS Monterey version 12.6 (21G115) on October 19, 2022.
> 
> -set sslrelax- not allowed; 'sslrelax' not recognized
> 
> *
> 
> I do not seem to be able to get past this download error, so I manually download the estimates’ zip files 
> from https://mrcdata.dide.ic.ac.uk/global-lmic-reports/ and save them in 
> CovidLongitudinal-main/code/download/IMPE
> 
> 
> * The BYPASS REMEDY is downloading the csv file and putting it in the directory "$pathCovidLongitudinal/download/IMPE"
> * to be used in this code here, plus /*commenting out*/ the code blocks for downloading and using the IMPE estimates: 
> 
> /*
> copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-04-28_v1.csv.zip 2020-04-28_v1.csv.zip
> unzipfile 2020-04-28_v1.csv.zip, replace
> erase 2020-04-28_v1.csv.zip
> */
> 
> 
> */
. 
. 
. 
. /*
> 
> * Turning off the BYPASS REMEDY is removing the  /*commented out*/ code blocks for downloading and using the IMPE estimates: 
> 
> From:
> 
> /*
> copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-04-28_v1.csv.zip 2020-04-28_v1.csv.zip
> unzipfile 2020-04-28_v1.csv.zip, replace
> erase 2020-04-28_v1.csv.zip
> */
> 
> 
> To:
> 
> copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-04-28_v1.csv.zip 2020-04-28_v1.csv.zip
> unzipfile 2020-04-28_v1.csv.zip, replace
> erase 2020-04-28_v1.csv.zip
> 
> 
> 
> * Also:
> 
> From:
> 
> shell rm -r "2020-04-28_v1.csv" 
> 
> 
> To:
> 
> shell rm -r "2020-04-28_v1.csv" 
> 
> 
> */
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. ****************************************************
. ****************************************************
. ****************************************************
. 
. 
.  
. /*
> 
> * IMPE updates all 
> 
> 
> copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/202?-??-??_v?.csv.zip 202?-??-??_v?.csv.zip
> 
> 
> 2020-04-28_v1.csv.zip
> 2020-04-29_v1.csv.zip
> 2020-04-30_v1.csv.zip
> 2020-05-01_v1.csv.zip
> 2020-05-02_v1.csv.zip
> 2020-05-03_v1.csv.zip
> 2020-05-04_v1.csv.zip
> 2020-05-05_v1.csv.zip
> 2020-05-06_v1.csv.zip
> 2020-05-07_v1.csv.zip
> 2020-05-08_v1.csv.zip
> 2020-05-09_v1.csv.zip
> 2020-05-10_v1.csv.zip
> 2020-05-11_v1.csv.zip
> 2020-05-12_v1.csv.zip
> 2020-05-13_v1.csv.zip
> 2020-05-14_v1.csv.zip
> 2020-05-15_v1.csv.zip
> 2020-05-16_v1.csv.zip
> 2020-05-17_v1.csv.zip
> 2020-05-19_v1.csv.zip
> 2020-05-20_v1.csv.zip
> 2020-05-21_v1.csv.zip
> 2020-05-22_v1.csv.zip
> 2020-05-23_v1.csv.zip
> 2020-05-24_v1.csv.zip
> 2020-05-25_v1.csv.zip
> 2020-05-26_v1.csv.zip
> 2020-05-27_v1.csv.zip
> 2020-05-28_v1.csv.zip
> 2020-05-29_v1.csv.zip
> 2020-05-30_v1.csv.zip
> 2020-05-31_v2.csv.zip
> 2020-06-01_v2.csv.zip
> 2020-06-02_v2.csv.zip
> 2020-06-03_v2.csv.zip
> 2020-06-04_v2.csv.zip
> 2020-06-06_v2.csv.zip
> 2020-06-09_v2.csv.zip
> 2020-06-14_v4.csv.zip
> 2020-06-16_v4.csv.zip
> 2020-06-19_v4.csv.zip
> 2020-06-21_v4.csv.zip
> 2020-06-23_v4.csv.zip
> 2020-06-26_v4.csv.zip
> 2020-06-28_v4.csv.zip
> 2020-07-02_v4.csv.zip
> 2020-07-03_v4.csv.zip
> 2020-07-04_v4.csv.zip
> 2020-07-07_v4.csv.zip
> 2020-07-11_v4.csv.zip
> 2020-07-14_v4.csv.zip
> 2020-07-15_v4.csv.zip
> 2020-07-18_v4.csv.zip
> 2020-07-20_v4.csv.zip
> 2020-07-22_v4.csv.zip
> 2020-07-25_v4.csv.zip
> 2020-07-27_v4.csv.zip
> 2020-07-31_v4.csv.zip
> 2020-08-08_v5.csv.zip
> 2020-08-10_v5.csv.zip
> 2020-08-11_v5.csv.zip
> 2020-08-13_v5.csv.zip
> 2020-08-14_v5.csv.zip
> 2020-08-17_v5.csv.zip
> 2020-08-23_v5.csv.zip
> 2020-08-25_v5.csv.zip
> 2020-08-27_v5.csv.zip
> 2020-08-31_v5.csv.zip
> 2020-09-02_v5.csv.zip
> 2020-09-06_v5.csv.zip
> 2020-09-08_v5.csv.zip
> 2020-09-12_v5.csv.zip
> 2020-09-15_v5.csv.zip
> 2020-09-19_v5.csv.zip
> 2020-09-23_v5.csv.zip
> 2020-09-25_v5.csv.zip
> 2020-10-03_v5.csv.zip
> 2020-10-06_v5.csv.zip
> 2020-10-12_v5.csv.zip
> 2020-10-19_v5.csv.zip
> 2020-10-25_v5.csv.zip
> 2020-10-28_v5.csv.zip
> 2020-11-01_v5.csv.zip
> 2020-11-10_v6.csv.zip
> 2020-11-14_v6.csv.zip
> 2020-11-18_v6.csv.zip
> 2020-11-23_v6.csv.zip
> 2020-11-29_v6.csv.zip
> 2020-12-04_v6.csv.zip
> 2020-12-12_v6.csv.zip
> 2020-12-20_v6.csv.zip
> 2020-12-26_v6.csv.zip
> 
> 
> 
> 2021-01-04_v6.csv.zip
> 2021-01-10_v6.csv.zip
> 2021-01-12_v6.csv.zip
> 2021-01-18_v7.csv.zip
> 2021-01-24_v7.csv.zip
> 2021-01-30_v7.csv.zip
> 2021-02-03_v7.csv.zip
> 2021-02-10_v7.csv.zip
> 2021-02-17_v7.csv.zip
> 2021-02-26_v7.csv.zip
> 2021-03-05_v7.csv.zip
> 2021-03-12_v7.csv.zip
> 2021-03-19_v7.csv.zip
> 2021-03-29_v7.csv.zip
> 2021-04-06_v7.csv.zip
> 2021-04-17_v7.csv.zip
> 2021-04-24_v7.csv.zip
> 2021-05-10_v8.csv.zip
> 2021-05-16_v8.csv.zip
> 2021-05-22_v8.csv.zip
> 2021-05-27_v8.csv.zip
> 2021-06-04_v8.csv.zip
> 2021-06-11_v8.csv.zip
> 2021-06-18_v8.csv.zip
> 2021-06-26_v8.csv.zip
> 2021-07-02_v8.csv.zip
> 2021-07-09_v8.csv.zip
> 2021-07-19_v8.csv.zip
> 2021-08-06_v8.csv.zip
> 2021-08-19_v8.csv.zip
> 2021-08-25_v8.csv.zip
> 2021-09-09_v8.csv.zip
> 2021-09-24_v8.csv.zip
> 2021-10-06_v8.csv.zip
> 2021-10-21_v8.csv.zip
> 2021-10-27_v8.csv.zip
> 2021-11-03_v9.csv.zip
> 2021-11-15_v9.csv.zip
> 2021-11-21_v9.csv.zip
> 2021-11-29_v9.csv.zip
> 2021-12-05_v9.csv.zip
> 2021-12-13_v9.csv.zip
> 2021-12-26_v9.csv.zip
> 
> 
> 2022-01-02_v9.csv.zip
> 2022-01-20_v9.csv.zip
> 2022-01-31_v9.csv.zip
> 2022-03-15_v9.csv.zip
> 2022-05-30_v9.csv.zip
> 
> 
> 
> https://github.com/mrc-ide/global-lmic-reports/tree/master/data
> 
> 
> 2022-06-20_v9.csv.zip
> 2022-07-03_v9.csv.zip
> 2022-07-12_v9.csv.zip
> 2022-07-28_v9.csv.zip
> 2022-08-08_v9.csv.zip
> 2022-09-01_v10.csv.zip
> 2022-12-25_v10.csv.zip
> 
> */
. 
. 
. 
. 
. * update 2020-04-28 20200428
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-04-28_v1.csv.zip 2020-04-28_v1.csv.zip
{txt}
{com}. unzipfile 2020-04-28_v1.csv.zip, replace
{res}{text:    inflating: 2020-04-28_v1.csv}

{text:successfully unzipped 2020-04-28_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-04-28_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-04-28_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 117,492 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(78,328 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(29,373 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21934{txt},{res}22061{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20jan2020{txt},{res}26may2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}128{col 51}{txt}Missing .: {res}0{txt}/{res}9,791

{txt}{col 19}Mean: {res}{ralign 7:22009.5}{txt} = {res}04apr2020{txt}(+ 13 hours)
{col 14}Std. dev.: {res}{ralign 7:30.4434}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21968}{ralign 11:   21984}{ralign 11:   22010}{ralign 11:   22036}{ralign 11:   22051}
{col 25}{ralign 9:23feb2020}{ralign 11:10mar2020}{ralign 11:05apr2020}{ralign 11:01may2020}{ralign 11:16may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(102 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(96 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(106 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(70 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(106 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200428
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200428.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200428.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200428.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-04-28_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-04-29 20200429
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-04-29_v1.csv.zip 2020-04-29_v1.csv.zip
{txt}
{com}. unzipfile 2020-04-29_v1.csv.zip, replace
{res}{text:    inflating: 2020-04-29_v1.csv}

{text:successfully unzipped 2020-04-29_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-04-29_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-04-29_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 119,160 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(79,440 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(29,790 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21935{txt},{res}22062{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}21jan2020{txt},{res}27may2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}128{col 51}{txt}Missing .: {res}0{txt}/{res}9,930

{txt}{col 19}Mean: {res}{ralign 7:22010.3}{txt} = {res}05apr2020{txt}(+ 8 hours)
{col 14}Std. dev.: {res}{ralign 7:30.6154}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21968}{ralign 11:   21985}{ralign 11:   22011}{ralign 11:   22037}{ralign 11:   22052}
{col 25}{ralign 9:23feb2020}{ralign 11:11mar2020}{ralign 11:06apr2020}{ralign 11:02may2020}{ralign 11:17may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(105 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(97 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(107 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(71 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(109 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(116 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200429
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200429.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200429.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200429.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-04-29_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-04-30 20200430
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-04-30_v1.csv.zip 2020-04-30_v1.csv.zip
{txt}
{com}. unzipfile 2020-04-30_v1.csv.zip, replace
{res}{text:    inflating: 2020-04-30_v1.csv}

{text:successfully unzipped 2020-04-30_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-04-30_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-04-30_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 120,060 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(80,040 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(30,015 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21933{txt},{res}22063{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}19jan2020{txt},{res}28may2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}131{col 51}{txt}Missing .: {res}0{txt}/{res}10,005

{txt}{col 19}Mean: {res}{ralign 7:22010.9}{txt} = {res}05apr2020{txt}(+ 23 hours)
{col 14}Std. dev.: {res}{ralign 7:30.8205}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21969}{ralign 11:   21985}{ralign 11:   22011}{ralign 11:   22037}{ralign 11:   22053}
{col 25}{ralign 9:24feb2020}{ralign 11:11mar2020}{ralign 11:06apr2020}{ralign 11:02may2020}{ralign 11:18may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(105 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(91 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(108 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(72 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(109 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(117 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200430
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200430.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200430.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200430.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-04-30_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-01 20200501
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-01_v1.csv.zip 2020-05-01_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-01_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-01_v1.csv}

{text:successfully unzipped 2020-05-01_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-01_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-01_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 124,344 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(82,896 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(31,086 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21929{txt},{res}22064{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}15jan2020{txt},{res}29may2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}136{col 51}{txt}Missing .: {res}0{txt}/{res}10,362

{txt}{col 19}Mean: {res}{ralign 7:22011.6}{txt} = {res}06apr2020{txt}(+ 14 hours)
{col 14}Std. dev.: {res}{ralign 7:31.1499}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21969}{ralign 11:   21986}{ralign 11:   22012}{ralign 11:   22038}{ralign 11:   22054}
{col 25}{ralign 9:24feb2020}{ralign 11:12mar2020}{ralign 11:07apr2020}{ralign 11:03may2020}{ralign 11:19may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(105 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(94 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(109 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(73 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(111 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(118 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200501
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200501.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200501.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200501.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-01_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-02 20200502
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-02_v1.csv.zip 2020-05-02_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-02_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-02_v1.csv}

{text:successfully unzipped 2020-05-02_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-02_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-02_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 124,920 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(83,280 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(31,230 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21933{txt},{res}22065{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}19jan2020{txt},{res}30may2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}133{col 51}{txt}Missing .: {res}0{txt}/{res}10,410

{txt}{col 19}Mean: {res}{ralign 7:22012.4}{txt} = {res}07apr2020{txt}(+ 9 hours)
{col 14}Std. dev.: {res}{ralign 7:31.2346}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21969}{ralign 11:   21986}{ralign 11:   22013}{ralign 11:   22039}{ralign 11:   22055}
{col 25}{ralign 9:24feb2020}{ralign 11:12mar2020}{ralign 11:08apr2020}{ralign 11:04may2020}{ralign 11:20may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(108 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(98 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(110 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(74 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(111 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(116 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200502
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200502.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200502.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200502.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-02_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-03 20200503
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-03_v1.csv.zip 2020-05-03_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-03_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-03_v1.csv}

{text:successfully unzipped 2020-05-03_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-03_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-03_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 125,940 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(83,960 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(31,485 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21936{txt},{res}22066{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}22jan2020{txt},{res}31may2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}131{col 51}{txt}Missing .: {res}0{txt}/{res}10,495

{txt}{col 19}Mean: {res}{ralign 7:22012.9}{txt} = {res}07apr2020{txt}(+ 22 hours)
{col 14}Std. dev.: {res}{ralign 7:31.5081}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21969}{ralign 11:   21987}{ralign 11:   22013}{ralign 11:   22040}{ralign 11:   22056}
{col 25}{ralign 9:24feb2020}{ralign 11:13mar2020}{ralign 11:08apr2020}{ralign 11:05may2020}{ralign 11:21may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(109 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(98 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(111 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(75 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(113 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(119 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200503
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200503.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200503.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200503.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-03_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-05-04 20200504
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-04_v1.csv.zip 2020-05-04_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-04_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-04_v1.csv}

{text:successfully unzipped 2020-05-04_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-04_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-04_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 127,392 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(84,928 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(31,848 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21935{txt},{res}22067{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}21jan2020{txt},{res}01jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}133{col 51}{txt}Missing .: {res}0{txt}/{res}10,616

{txt}{col 19}Mean: {res}{ralign 7:22013.8}{txt} = {res}08apr2020{txt}(+ 19 hours)
{col 14}Std. dev.: {res}{ralign 7:31.638}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21970}{ralign 11:   21988}{ralign 11:   22014}{ralign 11:   22041}{ralign 11:   22057}
{col 25}{ralign 9:25feb2020}{ralign 11:14mar2020}{ralign 11:09apr2020}{ralign 11:06may2020}{ralign 11:22may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(107 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(97 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(112 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(76 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(113 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(121 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200504
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200504.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200504.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200504.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-04_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-05-05 20200505
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-05_v1.csv.zip 2020-05-05_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-05_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-05_v1.csv}

{text:successfully unzipped 2020-05-05_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-05_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-05_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 128,376 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(85,584 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(32,094 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21936{txt},{res}22068{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}22jan2020{txt},{res}02jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}133{col 51}{txt}Missing .: {res}0{txt}/{res}10,698

{txt}{col 19}Mean: {res}{ralign 7:22014.8}{txt} = {res}09apr2020{txt}(+ 19 hours)
{col 14}Std. dev.: {res}{ralign 7:31.7586}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   21989}{ralign 11:   22016}{ralign 11:   22042}{ralign 11:   22058}
{col 25}{ralign 9:26feb2020}{ralign 11:15mar2020}{ralign 11:11apr2020}{ralign 11:07may2020}{ralign 11:23may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(108 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(95 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(113 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(77 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200505
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200505.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200505.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200505.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-05_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-06 20200506
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-06_v1.csv.zip 2020-05-06_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-06_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-06_v1.csv}

{text:successfully unzipped 2020-05-06_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-06_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-06_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 129,096 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(86,064 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(32,274 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21935{txt},{res}22069{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}21jan2020{txt},{res}03jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}135{col 51}{txt}Missing .: {res}0{txt}/{res}10,758

{txt}{col 19}Mean: {res}{ralign 7:22015.5}{txt} = {res}10apr2020{txt}(+ 11 hours)
{col 14}Std. dev.: {res}{ralign 7:31.9548}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   21989}{ralign 11:   22016}{ralign 11:   22043}{ralign 11:   22059}
{col 25}{ralign 9:26feb2020}{ralign 11:15mar2020}{ralign 11:11apr2020}{ralign 11:08may2020}{ralign 11:24may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(112 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(95 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(112 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(78 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(113 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(123 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200506
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200506.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200506.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200506.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-06_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-07 20200507
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-07_v1.csv.zip 2020-05-07_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-07_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-07_v1.csv}

{text:successfully unzipped 2020-05-07_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-07_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-07_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 130,428 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(86,952 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(32,607 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21933{txt},{res}22070{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}19jan2020{txt},{res}04jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}138{col 51}{txt}Missing .: {res}0{txt}/{res}10,869

{txt}{col 19}Mean: {res}{ralign 7:22015.9}{txt} = {res}10apr2020{txt}(+ 22 hours)
{col 14}Std. dev.: {res}{ralign 7:32.2647}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   21989}{ralign 11:   22017}{ralign 11:   22044}{ralign 11:   22060}
{col 25}{ralign 9:26feb2020}{ralign 11:15mar2020}{ralign 11:12apr2020}{ralign 11:09may2020}{ralign 11:25may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(109 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(99 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(79 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(117 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(124 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200507
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200507.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200507.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200507.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-07_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-08 20200508
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-08_v1.csv.zip 2020-05-08_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-08_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-08_v1.csv}

{text:successfully unzipped 2020-05-08_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-08_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-08_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 131,232 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(87,488 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(32,808 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21936{txt},{res}22071{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}22jan2020{txt},{res}05jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}136{col 51}{txt}Missing .: {res}0{txt}/{res}10,936

{txt}{col 19}Mean: {res}{ralign 7:22016.5}{txt} = {res}11apr2020{txt}(+ 13 hours)
{col 14}Std. dev.: {res}{ralign 7:32.5464}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   21990}{ralign 11:   22017}{ralign 11:   22044}{ralign 11:   22061}
{col 25}{ralign 9:26feb2020}{ralign 11:16mar2020}{ralign 11:12apr2020}{ralign 11:09may2020}{ralign 11:26may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(114 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(87 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(80 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(114 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(123 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200508
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200508.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200508.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200508.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-08_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-09 20200509
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-09_v1.csv.zip 2020-05-09_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-09_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-09_v1.csv}

{text:successfully unzipped 2020-05-09_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-09_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-09_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 131,916 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(87,944 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(32,979 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21936{txt},{res}22072{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}22jan2020{txt},{res}06jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}137{col 51}{txt}Missing .: {res}0{txt}/{res}10,993

{txt}{col 19}Mean: {res}{ralign 7:22017.3}{txt} = {res}12apr2020{txt}(+ 7 hours)
{col 14}Std. dev.: {res}{ralign 7:32.6682}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   21991}{ralign 11:   22018}{ralign 11:   22045}{ralign 11:   22062}
{col 25}{ralign 9:27feb2020}{ralign 11:17mar2020}{ralign 11:13apr2020}{ralign 11:10may2020}{ralign 11:27may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(114 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(96 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(117 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(81 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(114 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(124 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200509
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200509.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200509.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200509.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-09_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-05-10 20200510
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-10_v1.csv.zip 2020-05-10_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-10_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-10_v1.csv}

{text:successfully unzipped 2020-05-10_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-10_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-10_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 133,680 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(89,120 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(33,420 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21935{txt},{res}22073{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}21jan2020{txt},{res}07jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}139{col 51}{txt}Missing .: {res}0{txt}/{res}11,140

{txt}{col 19}Mean: {res}{ralign 7:22017.6}{txt} = {res}12apr2020{txt}(+ 15 hours)
{col 14}Std. dev.: {res}{ralign 7:32.976}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11: 21990.5}{ralign 11:   22018}{ralign 11:   22046}{ralign 11:   22062}
{col 25}{ralign 9:27feb2020}{ralign 11:16mar2020}{ralign 11:13apr2020}{ralign 11:11may2020}{ralign 11:27may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(114 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(98 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(116 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(82 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(127 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200510
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200510.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200510.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200510.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-10_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-11 20200511
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-11_v1.csv.zip 2020-05-11_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-11_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-11_v1.csv}

{text:successfully unzipped 2020-05-11_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-11_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-11_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 135,684 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(90,456 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(33,921 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21933{txt},{res}22074{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}19jan2020{txt},{res}08jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}142{col 51}{txt}Missing .: {res}0{txt}/{res}11,307

{txt}{col 19}Mean: {res}{ralign 7:22017.8}{txt} = {res}12apr2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:33.4082}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   21990}{ralign 11:   22019}{ralign 11:   22047}{ralign 11:   22063}
{col 25}{ralign 9:27feb2020}{ralign 11:16mar2020}{ralign 11:14apr2020}{ralign 11:12may2020}{ralign 11:28may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(109 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(119 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(83 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(121 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(127 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200511
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200511.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200511.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200511.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-11_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-12 20200512
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-12_v1.csv.zip 2020-05-12_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-12_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-12_v1.csv}

{text:successfully unzipped 2020-05-12_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-12_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-12_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 136,608 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(91,072 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(34,152 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21934{txt},{res}22075{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20jan2020{txt},{res}09jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}142{col 51}{txt}Missing .: {res}0{txt}/{res}11,384

{txt}{col 19}Mean: {res}{ralign 7:22018.4}{txt} = {res}13apr2020{txt}(+ 11 hours)
{col 14}Std. dev.: {res}{ralign 7:33.6288}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   21991}{ralign 11:   22019}{ralign 11:   22047}{ralign 11:   22064}
{col 25}{ralign 9:27feb2020}{ralign 11:17mar2020}{ralign 11:14apr2020}{ralign 11:12may2020}{ralign 11:29may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(114 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(110 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(84 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(122 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(128 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200512
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200512.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200512.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200512.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-12_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-13 20200513
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-13_v1.csv.zip 2020-05-13_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-13_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-13_v1.csv}

{text:successfully unzipped 2020-05-13_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-13_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-13_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 137,808 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(91,872 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(34,452 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21934{txt},{res}22076{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20jan2020{txt},{res}10jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}143{col 51}{txt}Missing .: {res}0{txt}/{res}11,484

{txt}{col 19}Mean: {res}{ralign 7:22019}{txt} = {res}13apr2020{txt}(+ 23 hours)
{col 14}Std. dev.: {res}{ralign 7:33.9173}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   21991}{ralign 11:   22020}{ralign 11:   22048}{ralign 11:   22065}
{col 25}{ralign 9:27feb2020}{ralign 11:17mar2020}{ralign 11:15apr2020}{ralign 11:13may2020}{ralign 11:30may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(118 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(111 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(114 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(84 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(122 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(129 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200513
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200513.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200513.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200513.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-13_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-14 20200514
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-14_v1.csv.zip 2020-05-14_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-14_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-14_v1.csv}

{text:successfully unzipped 2020-05-14_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-14_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-14_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 138,996 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(92,664 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(34,749 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21937{txt},{res}22077{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}23jan2020{txt},{res}11jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}141{col 51}{txt}Missing .: {res}0{txt}/{res}11,583

{txt}{col 19}Mean: {res}{ralign 7:22019.5}{txt} = {res}14apr2020{txt}(+ 11 hours)
{col 14}Std. dev.: {res}{ralign 7:34.2222}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   21991}{ralign 11:   22020}{ralign 11:   22049}{ralign 11:   22066}
{col 25}{ralign 9:27feb2020}{ralign 11:17mar2020}{ralign 11:15apr2020}{ralign 11:14may2020}{ralign 11:31may2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(112 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(118 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(86 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(138 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(130 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200514
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200514.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200514.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200514.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-14_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-15 20200515
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-15_v1.csv.zip 2020-05-15_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-15_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-15_v1.csv}

{text:successfully unzipped 2020-05-15_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-15_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-15_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 141,336 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(94,224 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(35,334 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21934{txt},{res}22078{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20jan2020{txt},{res}12jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}145{col 51}{txt}Missing .: {res}0{txt}/{res}11,778

{txt}{col 19}Mean: {res}{ralign 7:22020.1}{txt} = {res}15apr2020{txt}(+ 2 hours)
{col 14}Std. dev.: {res}{ralign 7:34.4065}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21973}{ralign 11:   21992}{ralign 11:   22021}{ralign 11:   22050}{ralign 11:   22067}
{col 25}{ralign 9:28feb2020}{ralign 11:18mar2020}{ralign 11:16apr2020}{ralign 11:15may2020}{ralign 11:01jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(121 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(112 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(117 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(87 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(138 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(132 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200515
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200515.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200515.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200515.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-15_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-16 20200516
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-16_v1.csv.zip 2020-05-16_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-16_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-16_v1.csv}

{text:successfully unzipped 2020-05-16_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-16_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-16_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 125,484 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(83,656 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(31,371 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21940{txt},{res}22079{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}26jan2020{txt},{res}13jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}140{col 51}{txt}Missing .: {res}0{txt}/{res}10,457

{txt}{col 19}Mean: {res}{ralign 7:22019.1}{txt} = {res}14apr2020{txt}(+ 2 hours)
{col 14}Std. dev.: {res}{ralign 7:35.2055}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   21989}{ralign 11:   22019}{ralign 11:   22049}{ralign 11:   22067}
{col 25}{ralign 9:26feb2020}{ralign 11:15mar2020}{ralign 11:14apr2020}{ralign 11:14may2020}{ralign 11:01jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(118 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(113 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(134 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(133 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200516
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200516.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200516.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200516.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-16_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-17 20200517
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-17_v1.csv.zip 2020-05-17_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-17_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-17_v1.csv}

{text:successfully unzipped 2020-05-17_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-17_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-17_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 144,684 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(96,456 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(36,171 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21938{txt},{res}22080{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24jan2020{txt},{res}14jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}143{col 51}{txt}Missing .: {res}0{txt}/{res}12,057

{txt}{col 19}Mean: {res}{ralign 7:22021.3}{txt} = {res}16apr2020{txt}(+ 7 hours)
{col 14}Std. dev.: {res}{ralign 7:34.8939}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21973}{ralign 11:   21992}{ralign 11:   22022}{ralign 11:   22051}{ralign 11:   22069}
{col 25}{ralign 9:28feb2020}{ralign 11:18mar2020}{ralign 11:17apr2020}{ralign 11:16may2020}{ralign 11:03jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(115 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(123 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(86 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(137 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(133 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200517
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200517.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200517.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200517.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-17_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-19 20200519
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-19_v1.csv.zip 2020-05-19_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-19_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-19_v1.csv}

{text:successfully unzipped 2020-05-19_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-19_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-19_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 147,576 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(98,384 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(36,894 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21938{txt},{res}22082{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24jan2020{txt},{res}16jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}145{col 51}{txt}Missing .: {res}0{txt}/{res}12,298

{txt}{col 19}Mean: {res}{ralign 7:22022.6}{txt} = {res}17apr2020{txt}(+ 15 hours)
{col 14}Std. dev.: {res}{ralign 7:35.313}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21974}{ralign 11:   21994}{ralign 11:   22023}{ralign 11:   22053}{ralign 11:   22071}
{col 25}{ralign 9:29feb2020}{ralign 11:20mar2020}{ralign 11:18apr2020}{ralign 11:18may2020}{ralign 11:05jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(122 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(117 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(86 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(141 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(136 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200519
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200519.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200519.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200519.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-19_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-05-20 20200520
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-20_v1.csv.zip 2020-05-20_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-20_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-20_v1.csv}

{text:successfully unzipped 2020-05-20_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-20_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-20_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 148,968 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(99,312 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(37,242 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21938{txt},{res}22083{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24jan2020{txt},{res}17jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}146{col 51}{txt}Missing .: {res}0{txt}/{res}12,414

{txt}{col 19}Mean: {res}{ralign 7:22023.1}{txt} = {res}18apr2020{txt}(+ 3 hours)
{col 14}Std. dev.: {res}{ralign 7:35.53}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21974}{ralign 11:   21994}{ralign 11:   22024}{ralign 11:   22054}{ralign 11:   22072}
{col 25}{ralign 9:29feb2020}{ralign 11:20mar2020}{ralign 11:19apr2020}{ralign 11:19may2020}{ralign 11:06jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(126 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(117 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(88 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(144 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(136 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200520
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200520.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200520.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200520.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-20_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-21 20200521
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-21_v1.csv.zip 2020-05-21_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-21_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-21_v1.csv}

{text:successfully unzipped 2020-05-21_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-21_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-21_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 149,784 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(99,856 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(37,446 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21940{txt},{res}22084{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}26jan2020{txt},{res}18jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}145{col 51}{txt}Missing .: {res}0{txt}/{res}12,482

{txt}{col 19}Mean: {res}{ralign 7:22023.8}{txt} = {res}18apr2020{txt}(+ 19 hours)
{col 14}Std. dev.: {res}{ralign 7:35.7476}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21974}{ralign 11:   21994}{ralign 11:   22024}{ralign 11:   22054}{ralign 11:   22072}
{col 25}{ralign 9:29feb2020}{ralign 11:20mar2020}{ralign 11:19apr2020}{ralign 11:19may2020}{ralign 11:06jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(125 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(117 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(121 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(92 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(140 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(137 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200521
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200521.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200521.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200521.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-21_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. * update 2020-05-22 20200522
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-22_v1.csv.zip 2020-05-22_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-22_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-22_v1.csv}

{text:successfully unzipped 2020-05-22_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-22_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-22_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 150,876 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(100,584 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(37,719 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21939{txt},{res}22085{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}25jan2020{txt},{res}19jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}147{col 51}{txt}Missing .: {res}0{txt}/{res}12,573

{txt}{col 19}Mean: {res}{ralign 7:22024.3}{txt} = {res}19apr2020{txt}(+ 8 hours)
{col 14}Std. dev.: {res}{ralign 7:36.0439}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21974}{ralign 11:   21995}{ralign 11:   22025}{ralign 11:   22055}{ralign 11:   22073}
{col 25}{ralign 9:29feb2020}{ralign 11:21mar2020}{ralign 11:20apr2020}{ralign 11:20may2020}{ralign 11:07jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(128 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(123 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(93 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(144 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(139 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200522
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200522.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200522.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200522.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-22_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-23 20200523
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-23_v1.csv.zip 2020-05-23_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-23_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-23_v1.csv}

{text:successfully unzipped 2020-05-23_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-23_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-23_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 151,860 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(101,240 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(37,965 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21938{txt},{res}22086{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24jan2020{txt},{res}20jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}149{col 51}{txt}Missing .: {res}0{txt}/{res}12,655

{txt}{col 19}Mean: {res}{ralign 7:22024.9}{txt} = {res}19apr2020{txt}(+ 22 hours)
{col 14}Std. dev.: {res}{ralign 7:36.2747}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   21995}{ralign 11:   22026}{ralign 11:   22056}{ralign 11:   22074}
{col 25}{ralign 9:01mar2020}{ralign 11:21mar2020}{ralign 11:21apr2020}{ralign 11:21may2020}{ralign 11:08jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(124 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(121 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(125 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(143 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(139 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200523
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200523.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200523.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200523.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-23_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-24 20200524
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-24_v1.csv.zip 2020-05-24_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-24_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-24_v1.csv}

{text:successfully unzipped 2020-05-24_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-24_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-24_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 154,368 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(102,912 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(38,592 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21940{txt},{res}22087{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}26jan2020{txt},{res}21jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}148{col 51}{txt}Missing .: {res}0{txt}/{res}12,864

{txt}{col 19}Mean: {res}{ralign 7:22025.5}{txt} = {res}20apr2020{txt}(+ 12 hours)
{col 14}Std. dev.: {res}{ralign 7:36.5143}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   21995}{ralign 11:   22026}{ralign 11:   22057}{ralign 11:   22075}
{col 25}{ralign 9:01mar2020}{ralign 11:21mar2020}{ralign 11:21apr2020}{ralign 11:22may2020}{ralign 11:09jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(127 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(120 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(129 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(94 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(140 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(135 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200524
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200524.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200524.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200524.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-24_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-25 20200525
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-25_v1.csv.zip 2020-05-25_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-25_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-25_v1.csv}

{text:successfully unzipped 2020-05-25_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-25_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-25_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 156,096 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(104,064 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(39,024 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21939{txt},{res}22088{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}25jan2020{txt},{res}22jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}150{col 51}{txt}Missing .: {res}0{txt}/{res}13,008

{txt}{col 19}Mean: {res}{ralign 7:22025.8}{txt} = {res}20apr2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:36.9274}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   21995}{ralign 11:   22027}{ralign 11:   22058}{ralign 11:   22076}
{col 25}{ralign 9:01mar2020}{ralign 11:21mar2020}{ralign 11:22apr2020}{ralign 11:23may2020}{ralign 11:10jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(127 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(123 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(129 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(92 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(147 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(141 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200525
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200525.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200525.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200525.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-25_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-26 20200526
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-26_v1.csv.zip 2020-05-26_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-26_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-26_v1.csv}

{text:successfully unzipped 2020-05-26_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-26_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-26_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 158,268 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(105,512 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(39,567 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21940{txt},{res}22089{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}26jan2020{txt},{res}23jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}150{col 51}{txt}Missing .: {res}0{txt}/{res}13,189

{txt}{col 19}Mean: {res}{ralign 7:22026.6}{txt} = {res}21apr2020{txt}(+ 14 hours)
{col 14}Std. dev.: {res}{ralign 7:37.0413}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   21996}{ralign 11:   22027}{ralign 11:   22058}{ralign 11:   22077}
{col 25}{ralign 9:01mar2020}{ralign 11:22mar2020}{ralign 11:22apr2020}{ralign 11:23may2020}{ralign 11:11jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(132 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(121 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(125 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(93 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(140 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(143 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200526
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200526.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200526.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200526.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-26_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-27 20200527
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-27_v1.csv.zip 2020-05-27_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-27_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-27_v1.csv}

{text:successfully unzipped 2020-05-27_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-27_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-27_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 159,684 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(106,456 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(39,921 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21943{txt},{res}22090{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}29jan2020{txt},{res}24jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}148{col 51}{txt}Missing .: {res}0{txt}/{res}13,307

{txt}{col 19}Mean: {res}{ralign 7:22027}{txt} = {res}22apr2020{txt}(+ 1 hour)
{col 14}Std. dev.: {res}{ralign 7:37.3569}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   21996}{ralign 11:   22028}{ralign 11:   22059}{ralign 11:   22078}
{col 25}{ralign 9:01mar2020}{ralign 11:22mar2020}{ralign 11:23apr2020}{ralign 11:24may2020}{ralign 11:12jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(132 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(119 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(126 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(96 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(142 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(144 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200527
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200527.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200527.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200527.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-27_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-05-28 20200528
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-28_v1.csv.zip 2020-05-28_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-28_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-28_v1.csv}

{text:successfully unzipped 2020-05-28_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-28_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-28_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 161,052 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(107,368 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(40,263 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21943{txt},{res}22091{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}29jan2020{txt},{res}25jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}149{col 51}{txt}Missing .: {res}0{txt}/{res}13,421

{txt}{col 19}Mean: {res}{ralign 7:22027.5}{txt} = {res}22apr2020{txt}(+ 12 hours)
{col 14}Std. dev.: {res}{ralign 7:37.6519}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   21996}{ralign 11:   22028}{ralign 11:   22060}{ralign 11:   22079}
{col 25}{ralign 9:01mar2020}{ralign 11:22mar2020}{ralign 11:23apr2020}{ralign 11:25may2020}{ralign 11:13jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(126 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(116 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(130 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(93 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(143 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(138 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200528
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200528.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200528.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200528.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-28_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-05-29 20200529
. 
. ******************************
. 
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-29_v1.csv.zip 2020-05-29_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-29_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-29_v1.csv}

{text:successfully unzipped 2020-05-29_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-29_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-29_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 161,916 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(107,944 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(40,479 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21943{txt},{res}22092{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}29jan2020{txt},{res}26jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}150{col 51}{txt}Missing .: {res}0{txt}/{res}13,493

{txt}{col 19}Mean: {res}{ralign 7:22028.1}{txt} = {res}23apr2020{txt}(+ 3 hours)
{col 14}Std. dev.: {res}{ralign 7:37.9078}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21976}{ralign 11:   21997}{ralign 11:   22029}{ralign 11:   22061}{ralign 11:   22080}
{col 25}{ralign 9:02mar2020}{ralign 11:23mar2020}{ralign 11:24apr2020}{ralign 11:26may2020}{ralign 11:14jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(132 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(122 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(126 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(92 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(148 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(145 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200529
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200529.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200529.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200529.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-29_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. 
. * update 2020-05-30 20200530
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-30_v1.csv.zip 2020-05-30_v1.csv.zip
{txt}
{com}. unzipfile 2020-05-30_v1.csv.zip, replace
{res}{text:    inflating: 2020-05-30_v1.csv}

{text:successfully unzipped 2020-05-30_v1.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-30_v1.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-30_v1.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 163,296 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(108,864 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(40,824 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21942{txt},{res}22093{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}28jan2020{txt},{res}27jun2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}152{col 51}{txt}Missing .: {res}0{txt}/{res}13,608

{txt}{col 19}Mean: {res}{ralign 7:22028.6}{txt} = {res}23apr2020{txt}(+ 14 hours)
{col 14}Std. dev.: {res}{ralign 7:38.2151}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21976}{ralign 11:   21997}{ralign 11:   22029}{ralign 11:   22061}{ralign 11:   22081}
{col 25}{ralign 9:02mar2020}{ralign 11:23mar2020}{ralign 11:24apr2020}{ralign 11:26may2020}{ralign 11:15jun2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(136 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(122 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(133 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(95 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(148 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(143 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200530
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200530.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200530.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200530.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-30_v1.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-05-31 20200531
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-05-31_v2.csv.zip 2020-05-31_v2.csv.zip
{txt}
{com}. unzipfile 2020-05-31_v2.csv.zip, replace
{res}{text:    inflating: 2020-05-31_v2.csv}

{text:successfully unzipped 2020-05-31_v2.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-05-31_v2.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-05-31_v2.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,206,810 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,005,645 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(178,801 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21927{txt},{res}22155{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}13jan2020{txt},{res}28aug2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}229{col 51}{txt}Missing .: {res}0{txt}/{res}22,364

{txt}{col 19}Mean: {res}{ralign 7:22053.9}{txt} = {res}18may2020{txt}(+ 22 hours)
{col 14}Std. dev.: {res}{ralign 7:59.5101}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   22004}{ralign 11:   22055}{ralign 11:   22105}{ralign 11:   22135}
{col 25}{ralign 9:26feb2020}{ralign 11:30mar2020}{ralign 11:20may2020}{ralign 11:09jul2020}{ralign 11:08aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(174 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(204 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(215 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(179 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(204 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(210 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200531
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200531.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200531.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200531.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-05-31_v2.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-06-01 20200601
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-01_v2.csv.zip 2020-06-01_v2.csv.zip
{txt}
{com}. unzipfile 2020-06-01_v2.csv.zip, replace
{res}{text:    inflating: 2020-06-01_v2.csv}

{text:successfully unzipped 2020-06-01_v2.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-01_v2.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-01_v2.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,726,155 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,438,626 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(255,564 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22156{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}29aug2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}254{col 51}{txt}Missing .: {res}0{txt}/{res}31,965

{txt}{col 19}Mean: {res}{ralign 7:22053.3}{txt} = {res}18may2020{txt}(+ 6 hours)
{col 14}Std. dev.: {res}{ralign 7:60.3923}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21969}{ralign 11:   22003}{ralign 11:   22054}{ralign 11:   22105}{ralign 11:   22136}
{col 25}{ralign 9:24feb2020}{ralign 11:29mar2020}{ralign 11:19may2020}{ralign 11:09jul2020}{ralign 11:09aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(149 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(175 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(206 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(216 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(180 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(205 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(211 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(216 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200601
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200601.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200601.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200601.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-01_v2.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-06-02 20200602
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-02_v2.csv.zip 2020-06-02_v2.csv.zip
{txt}
{com}. unzipfile 2020-06-02_v2.csv.zip, replace
{res}{text:    inflating: 2020-06-02_v2.csv}

{text:successfully unzipped 2020-06-02_v2.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-02_v2.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-02_v2.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,217,286 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,014,330 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(180,393 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21925{txt},{res}22157{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}11jan2020{txt},{res}30aug2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}233{col 51}{txt}Missing .: {res}0{txt}/{res}22,563

{txt}{col 19}Mean: {res}{ralign 6:22055}{txt} = {res}20may2020{txt}(+ 0 hours)
{col 14}Std. dev.: {res}{ralign 6:60.002}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   22005}{ralign 11:   22056}{ralign 11:   22107}{ralign 11:   22137}
{col 25}{ralign 9:26feb2020}{ralign 11:31mar2020}{ralign 11:21may2020}{ralign 11:11jul2020}{ralign 11:10aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(176 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(207 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(217 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(181 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(206 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(212 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200602
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200602.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200602.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200602.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-02_v2.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-06-03 20200603
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-03_v2.csv.zip 2020-06-03_v2.csv.zip
{txt}
{com}. unzipfile 2020-06-03_v2.csv.zip, replace
{res}{text:    inflating: 2020-06-03_v2.csv}

{text:successfully unzipped 2020-06-03_v2.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-03_v2.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-03_v2.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,717,683 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,431,529 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(254,342 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22158{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}31aug2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}256{col 51}{txt}Missing .: {res}0{txt}/{res}31,812

{txt}{col 19}Mean: {res}{ralign 7:22054.4}{txt} = {res}19may2020{txt}(+ 11 hours)
{col 14}Std. dev.: {res}{ralign 7:60.8539}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21970}{ralign 11:   22003}{ralign 11:   22055}{ralign 11:   22107}{ralign 11:   22138}
{col 25}{ralign 9:25feb2020}{ralign 11:29mar2020}{ralign 11:20may2020}{ralign 11:11jul2020}{ralign 11:11aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(151 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(177 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(208 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(218 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(182 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(205 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(213 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(217 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200603
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200603.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200603.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200603.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-03_v2.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-06-04 20200604
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-04_v2.csv.zip 2020-06-04_v2.csv.zip
{txt}
{com}. unzipfile 2020-06-04_v2.csv.zip, replace
{res}{text:    inflating: 2020-06-04_v2.csv}

{text:successfully unzipped 2020-06-04_v2.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-04_v2.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-04_v2.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,229,004 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,024,122 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(182,105 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21930{txt},{res}22159{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}16jan2020{txt},{res}01sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}230{col 51}{txt}Missing .: {res}0{txt}/{res}22,777

{txt}{col 19}Mean: {res}{ralign 7:22056.1}{txt} = {res}21may2020{txt}(+ 2 hours)
{col 14}Std. dev.: {res}{ralign 7:60.5376}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   22006}{ralign 11:   22057}{ralign 11:   22108}{ralign 11:   22139}
{col 25}{ralign 9:27feb2020}{ralign 11:01apr2020}{ralign 11:22may2020}{ralign 11:12jul2020}{ralign 11:12aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(178 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(209 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(219 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(183 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(205 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(214 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200604
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200604.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200604.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200604.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-04_v2.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. * update 2020-06-06 20200606
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-06_v2.csv.zip 2020-06-06_v2.csv.zip
{txt}
{com}. unzipfile 2020-06-06_v2.csv.zip, replace
{res}{text:    inflating: 2020-06-06_v2.csv}

{text:successfully unzipped 2020-06-06_v2.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-06_v2.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-06_v2.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,777,425 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,481,338 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(263,171 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22161{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}03sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}259{col 51}{txt}Missing .: {res}0{txt}/{res}32,916

{txt}{col 19}Mean: {res}{ralign 7:22055.9}{txt} = {res}20may2020{txt}(+ 22 hours)
{col 14}Std. dev.: {res}{ralign 7:61.7269}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21970}{ralign 11:   22004}{ralign 11:   22057}{ralign 11:   22109}{ralign 11:   22141}
{col 25}{ralign 9:25feb2020}{ralign 11:30mar2020}{ralign 11:22may2020}{ralign 11:13jul2020}{ralign 11:14aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(154 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(180 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(211 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(221 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(185 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(206 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(215 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(220 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200606
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200606.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200606.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200606.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-06_v2.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. * update 2020-06-09 20200609
. 
. ******************************
. 
. 
. 
. * get csv file 
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-09_v2.csv.zip 2020-06-09_v2.csv.zip
{txt}
{com}. unzipfile 2020-06-09_v2.csv.zip, replace
{res}{text:    inflating: 2020-06-09_v2.csv}

{text:successfully unzipped 2020-06-09_v2.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-09_v2.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-09_v2.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,253,790 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,045,011 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(185,569 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21931{txt},{res}22164{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}17jan2020{txt},{res}06sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}234{col 51}{txt}Missing .: {res}0{txt}/{res}23,210

{txt}{col 19}Mean: {res}{ralign 7:22059.1}{txt} = {res}24may2020{txt}(+ 2 hours)
{col 14}Std. dev.: {res}{ralign 7:61.7335}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21973}{ralign 11:   22008}{ralign 11:   22060}{ralign 11:   22112}{ralign 11:   22144}
{col 25}{ralign 9:28feb2020}{ralign 11:03apr2020}{ralign 11:25may2020}{ralign 11:16jul2020}{ralign 11:17aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(183 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(214 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(224 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(188 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(203 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(219 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200609
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200609.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200609.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200609.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-09_v2.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. * update 2020-06-14 20200614
. 
. ******************************
. 
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-14_v4.csv.zip 2020-06-14_v4.csv.zip
{txt}
{com}. unzipfile 2020-06-14_v4.csv.zip, replace
{res}{text:    inflating: 2020-06-14_v4.csv}

{text:successfully unzipped 2020-06-14_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-14_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-14_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,827,621 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,522,956 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(270,796 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22169{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}11sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}267{col 51}{txt}Missing .: {res}0{txt}/{res}33,869

{txt}{col 19}Mean: {res}{ralign 7:22060.1}{txt} = {res}25may2020{txt}(+ 3 hours)
{col 14}Std. dev.: {res}{ralign 7:63.9415}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21971}{ralign 11:   22007}{ralign 11:   22061}{ralign 11:   22115}{ralign 11:   22148}
{col 25}{ralign 9:26feb2020}{ralign 11:02apr2020}{ralign 11:26may2020}{ralign 11:19jul2020}{ralign 11:21aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(162 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(188 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(219 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(229 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(193 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(217 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(223 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(229 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200614
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200614.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200614.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200614.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-14_v4.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
.  
. * update 2020-06-16 20200616
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-16_v4.csv.zip 2020-06-16_v4.csv.zip
{txt}
{com}. unzipfile 2020-06-16_v4.csv.zip, replace
{res}{text:    inflating: 2020-06-16_v4.csv}

{text:successfully unzipped 2020-06-16_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-16_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-16_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,856,454 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,547,029 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(275,027 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22171{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}13sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}269{col 51}{txt}Missing .: {res}0{txt}/{res}34,398

{txt}{col 19}Mean: {res}{ralign 7:22061.2}{txt} = {res}26may2020{txt}(+ 4 hours)
{col 14}Std. dev.: {res}{ralign 7:64.487}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   22007}{ralign 11:   22062}{ralign 11:   22117}{ralign 11:   22150}
{col 25}{ralign 9:27feb2020}{ralign 11:02apr2020}{ralign 11:27may2020}{ralign 11:21jul2020}{ralign 11:23aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(164 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(190 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(221 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(231 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(195 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(218 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(225 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(232 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200616
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200616.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200616.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200616.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-16_v4.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. *
. 
. * update 2020-06-19 20200619
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-19_v4.csv.zip 2020-06-19_v4.csv.zip
{txt}
{com}. unzipfile 2020-06-19_v4.csv.zip, replace
{res}{text:    inflating: 2020-06-19_v4.csv}

{text:successfully unzipped 2020-06-19_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-19_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-19_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,866,906 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,555,986 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(276,356 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22174{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}16sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}272{col 51}{txt}Missing .: {res}0{txt}/{res}34,564

{txt}{col 19}Mean: {res}{ralign 7:22063}{txt} = {res}27may2020{txt}(+ 23 hours)
{col 14}Std. dev.: {res}{ralign 7:65.1703}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21972}{ralign 11:   22008}{ralign 11:   22064}{ralign 11:   22119}{ralign 11:   22152}
{col 25}{ralign 9:27feb2020}{ralign 11:03apr2020}{ralign 11:29may2020}{ralign 11:23jul2020}{ralign 11:25aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(167 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(193 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(224 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(234 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(198 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(220 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(228 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(235 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200619
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200619.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200619.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200619.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-19_v4.csv" 
{txt}

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. *
. 
. * update 2020-06-21 20200621
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-21_v4.csv.zip 2020-06-21_v4.csv.zip
{txt}
{com}. unzipfile 2020-06-21_v4.csv.zip, replace
{res}{text:    inflating: 2020-06-21_v4.csv}

{text:successfully unzipped 2020-06-21_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-21_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-21_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,318,833 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,099,110 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(195,297 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21930{txt},{res}22176{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}16jan2020{txt},{res}18sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}247{col 51}{txt}Missing .: {res}0{txt}/{res}24,426

{txt}{col 19}Mean: {res}{ralign 7:22065.5}{txt} = {res}30may2020{txt}(+ 13 hours)
{col 14}Std. dev.: {res}{ralign 7:64.9665}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   22011}{ralign 11:   22066}{ralign 11:   22121}{ralign 11:   22154}
{col 25}{ralign 9:01mar2020}{ralign 11:06apr2020}{ralign 11:31may2020}{ralign 11:25jul2020}{ralign 11:27aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(195 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(226 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(236 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(200 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(219 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(230 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200621
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200621.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200621.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200621.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-21_v4.csv.zip" 
{txt}
rm: 2020-06-21_v4.csv.zip: No such file or directory

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. 
. *
. 
. * update 2020-06-23 20200623
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-23_v4.csv.zip 2020-06-23_v4.csv.zip
{txt}
{com}. unzipfile 2020-06-23_v4.csv.zip, replace
{res}{text:    inflating: 2020-06-23_v4.csv}

{text:successfully unzipped 2020-06-23_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-23_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-23_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,911,264 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,592,731 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(283,123 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22178{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}20sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}276{col 51}{txt}Missing .: {res}0{txt}/{res}35,410

{txt}{col 19}Mean: {res}{ralign 7:22064.9}{txt} = {res}29may2020{txt}(+ 21 hours)
{col 14}Std. dev.: {res}{ralign 7:66.4326}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21973}{ralign 11:   22009}{ralign 11:   22066}{ralign 11:   22122}{ralign 11:   22156}
{col 25}{ralign 9:28feb2020}{ralign 11:04apr2020}{ralign 11:31may2020}{ralign 11:26jul2020}{ralign 11:29aug2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(170 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(197 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(228 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(238 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(202 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(220 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(233 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(237 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200623
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200623.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200623.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200623.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-23_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. 
. 
. 
. 
. *
. 
. * update 2020-06-26 20200626
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-26_v4.csv.zip 2020-06-26_v4.csv.zip
{txt}
{com}. unzipfile 2020-06-26_v4.csv.zip, replace
{res}{text:    inflating: 2020-06-26_v4.csv}

{text:successfully unzipped 2020-06-26_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-26_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-26_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,346,886 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,122,195 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(199,713 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21931{txt},{res}22181{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}17jan2020{txt},{res}23sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}251{col 51}{txt}Missing .: {res}0{txt}/{res}24,978

{txt}{col 19}Mean: {res}{ralign 7:22068.1}{txt} = {res}02jun2020{txt}(+ 2 hours)
{col 14}Std. dev.: {res}{ralign 7:66.3828}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21976}{ralign 11:   22012}{ralign 11:   22069}{ralign 11:   22125}{ralign 11:   22159}
{col 25}{ralign 9:02mar2020}{ralign 11:07apr2020}{ralign 11:03jun2020}{ralign 11:29jul2020}{ralign 11:01sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(200 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(231 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(241 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(205 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(224 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(236 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200626
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200626.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200626.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200626.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-26_v4.csv.zip" 
{txt}
rm: 2020-06-26_v4.csv.zip: No such file or directory

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. 
. *
. 
. * update 2020-06-28 20200628
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-06-28_v4.csv.zip 2020-06-28_v4.csv.zip
{txt}
{com}. unzipfile 2020-06-28_v4.csv.zip, replace
{res}{text:    inflating: 2020-06-28_v4.csv}

{text:successfully unzipped 2020-06-28_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-06-28_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-06-28_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,357,902 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,131,654 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(201,097 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21930{txt},{res}22183{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}16jan2020{txt},{res}25sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}254{col 51}{txt}Missing .: {res}0{txt}/{res}25,151

{txt}{col 19}Mean: {res}{ralign 7:22069.3}{txt} = {res}03jun2020{txt}(+ 8 hours)
{col 14}Std. dev.: {res}{ralign 7:66.7922}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21976}{ralign 11:   22013}{ralign 11:   22070}{ralign 11:   22127}{ralign 11:   22161}
{col 25}{ralign 9:02mar2020}{ralign 11:08apr2020}{ralign 11:04jun2020}{ralign 11:31jul2020}{ralign 11:03sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(202 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(233 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(243 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(207 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(231 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(238 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200628
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200628.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200628.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200628.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-06-28_v4.csv.zip" 
{txt}
rm: 2020-06-28_v4.csv.zip: No such file or directory

{com}. 
. ******
. 
. *
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. * update 2020-07-02 20200702
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-02_v4.csv.zip 2020-07-02_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-02_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-02_v4.csv}

{text:successfully unzipped 2020-07-02_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-02_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-02_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,995,312 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,662,803 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(295,546 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22187{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}29sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}285{col 51}{txt}Missing .: {res}0{txt}/{res}36,963

{txt}{col 19}Mean: {res}{ralign 7:22069.7}{txt} = {res}03jun2020{txt}(+ 17 hours)
{col 14}Std. dev.: {res}{ralign 7:68.8179}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21974}{ralign 11:   22012}{ralign 11:   22071}{ralign 11:   22129}{ralign 11:   22164}
{col 25}{ralign 9:29feb2020}{ralign 11:07apr2020}{ralign 11:05jun2020}{ralign 11:02aug2020}{ralign 11:06sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(180 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(206 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(237 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(247 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(211 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(232 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(229 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(245 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200702
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200702.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200702.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200702.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-02_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-03 20200703
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-03_v4.csv.zip 2020-07-03_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-03_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-03_v4.csv}

{text:successfully unzipped 2020-07-03_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-03_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-03_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,002,899 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,669,238 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(296,570 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22188{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}30sep2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}286{col 51}{txt}Missing .: {res}0{txt}/{res}37,091

{txt}{col 19}Mean: {res}{ralign 7:22070.3}{txt} = {res}04jun2020{txt}(+ 8 hours)
{col 14}Std. dev.: {res}{ralign 7:69.0163}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   22012}{ralign 11:   22071}{ralign 11:   22130}{ralign 11:   22165}
{col 25}{ralign 9:01mar2020}{ralign 11:07apr2020}{ralign 11:05jun2020}{ralign 11:03aug2020}{ralign 11:07sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(181 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(207 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(238 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(248 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(212 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(229 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(231 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(249 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200703
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200703.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200703.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200703.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-03_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-04 20200704
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-04_v4.csv.zip 2020-07-04_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-04_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-04_v4.csv}

{text:successfully unzipped 2020-07-04_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-04_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-04_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,010,972 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,675,880 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(297,842 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22189{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}01oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}287{col 51}{txt}Missing .: {res}0{txt}/{res}37,250

{txt}{col 19}Mean: {res}{ralign 7:22070.8}{txt} = {res}04jun2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:69.3195}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   22013}{ralign 11:   22072}{ralign 11:   22131}{ralign 11:   22166}
{col 25}{ralign 9:01mar2020}{ralign 11:08apr2020}{ralign 11:06jun2020}{ralign 11:04aug2020}{ralign 11:08sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(182 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(208 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(239 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(249 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(213 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(230 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(227 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(247 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
.  
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200704
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200704.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200704.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200704.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-04_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-07 20200707
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-07_v4.csv.zip 2020-07-07_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-07_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-07_v4.csv}

{text:successfully unzipped 2020-07-07_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-07_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-07_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,019,588 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,682,938 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(299,227 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22192{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}04oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}290{col 51}{txt}Missing .: {res}0{txt}/{res}37,423

{txt}{col 19}Mean: {res}{ralign 7:22072.5}{txt} = {res}06jun2020{txt}(+ 13 hours)
{col 14}Std. dev.: {res}{ralign 7:70.0458}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21975}{ralign 11:   22014}{ralign 11:   22073}{ralign 11:   22133}{ralign 11:   22169}
{col 25}{ralign 9:01mar2020}{ralign 11:09apr2020}{ralign 11:07jun2020}{ralign 11:06aug2020}{ralign 11:11sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(185 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(211 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(242 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(252 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(216 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(231 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(228 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(251 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200707
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200707.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200707.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200707.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-04.csv" 
{txt}
rm: 2020-07-04.csv: No such file or directory

{com}. 
. ******
. 
. 
. 
. * update 2020-07-11 20200711
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-11_v4.csv.zip 2020-07-11_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-11_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-11_v4.csv}

{text:successfully unzipped 2020-07-11_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-11_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-11_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,053,443 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,711,196 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(304,202 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22196{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}08oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}294{col 51}{txt}Missing .: {res}0{txt}/{res}38,045

{txt}{col 19}Mean: {res}{ralign 7:22075.1}{txt} = {res}09jun2020{txt}(+ 2 hours)
{col 14}Std. dev.: {res}{ralign 7:71.0503}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21976}{ralign 11:   22015}{ralign 11:   22076}{ralign 11:   22136}{ralign 11:   22172}
{col 25}{ralign 9:02mar2020}{ralign 11:10apr2020}{ralign 11:10jun2020}{ralign 11:09aug2020}{ralign 11:14sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(188 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(215 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(246 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(256 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(220 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(238 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(232 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(254 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200711
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200711.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200711.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200711.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-11_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-14 20200714
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-14_v4.csv.zip 2020-07-14_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-14_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-14_v4.csv}

{text:successfully unzipped 2020-07-14_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-14_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-14_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,106,816 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,755,679 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(312,104 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22199{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}11oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}297{col 51}{txt}Missing .: {res}0{txt}/{res}39,033

{txt}{col 19}Mean: {res}{ralign 7:22076.5}{txt} = {res}10jun2020{txt}(+ 12 hours)
{col 14}Std. dev.: {res}{ralign 7:71.9477}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21977}{ralign 11:   22016}{ralign 11:   22078}{ralign 11:   22139}{ralign 11:   22175}
{col 25}{ralign 9:03mar2020}{ralign 11:11apr2020}{ralign 11:12jun2020}{ralign 11:12aug2020}{ralign 11:17sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(192 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(218 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(249 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(259 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(223 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(240 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(229 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(257 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200714
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200714.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200714.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200714.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-14_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-15 20200715
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-15_v4.csv.zip 2020-07-15_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-15_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-15_v4.csv}

{text:successfully unzipped 2020-07-15_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-15_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-15_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,115,618 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,763,113 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(313,320 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22200{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}12oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}298{col 51}{txt}Missing .: {res}0{txt}/{res}39,185

{txt}{col 19}Mean: {res}{ralign 7:22077}{txt} = {res}11jun2020{txt}(+ 1 hour)
{col 14}Std. dev.: {res}{ralign 7:72.2253}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21977}{ralign 11:   22016}{ralign 11:   22078}{ralign 11:   22139}{ralign 11:   22176}
{col 25}{ralign 9:03mar2020}{ralign 11:11apr2020}{ralign 11:12jun2020}{ralign 11:12aug2020}{ralign 11:18sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(193 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(219 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(250 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(260 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(224 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(240 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(234 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(258 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200715
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200715.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200715.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200715.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-15_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-18 20200718
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-18_v4.csv.zip 2020-07-18_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-18_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-18_v4.csv}

{text:successfully unzipped 2020-07-18_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-18_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-18_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,140,269 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,783,606 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(317,016 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22203{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}15oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}301{col 51}{txt}Missing .: {res}0{txt}/{res}39,647

{txt}{col 19}Mean: {res}{ralign 7:22078.6}{txt} = {res}12jun2020{txt}(+ 15 hours)
{col 14}Std. dev.: {res}{ralign 7:73.0523}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21977}{ralign 11:   22017}{ralign 11:   22080}{ralign 11:   22142}{ralign 11:   22179}
{col 25}{ralign 9:03mar2020}{ralign 11:12apr2020}{ralign 11:14jun2020}{ralign 11:15aug2020}{ralign 11:21sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(196 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(222 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(253 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(263 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(227 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(238 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(237 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(261 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
.  
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200718
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200718.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200718.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200718.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-18_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-20 20200720
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-20_v4.csv.zip 2020-07-20_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-20_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-20_v4.csv}

{text:successfully unzipped 2020-07-20_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-20_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-20_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,155,392 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,796,299 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(319,148 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22205{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}17oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}303{col 51}{txt}Missing .: {res}0{txt}/{res}39,945

{txt}{col 19}Mean: {res}{ralign 7:22079.7}{txt} = {res}13jun2020{txt}(+ 17 hours)
{col 14}Std. dev.: {res}{ralign 7:73.5425}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21978}{ralign 11:   22018}{ralign 11:   22081}{ralign 11:   22143}{ralign 11:   22181}
{col 25}{ralign 9:04mar2020}{ralign 11:13apr2020}{ralign 11:15jun2020}{ralign 11:16aug2020}{ralign 11:23sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(197 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(224 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(255 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(265 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(229 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(240 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(237 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(264 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200720
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200720.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200720.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200720.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-20_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-22 20200722
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-22_v4.csv.zip 2020-07-22_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-22_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-22_v4.csv}

{text:successfully unzipped 2020-07-22_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-22_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-22_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,174,073 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,811,695 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(322,096 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22207{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}19oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}305{col 51}{txt}Missing .: {res}0{txt}/{res}40,282

{txt}{col 19}Mean: {res}{ralign 7:22080.7}{txt} = {res}14jun2020{txt}(+ 16 hours)
{col 14}Std. dev.: {res}{ralign 7:74.1559}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21978}{ralign 11:   22018}{ralign 11:   22082}{ralign 11:   22145}{ralign 11:   22182}
{col 25}{ralign 9:04mar2020}{ralign 11:13apr2020}{ralign 11:16jun2020}{ralign 11:18aug2020}{ralign 11:24sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(200 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(226 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(257 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(267 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(231 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(241 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(241 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(264 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200722
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200722.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200722.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200722.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-22_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-25 20200725
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-25_v4.csv.zip 2020-07-25_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-25_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-25_v4.csv}

{text:successfully unzipped 2020-07-25_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-25_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-25_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,206,821 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,839,017 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(326,919 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22210{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}22oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}308{col 51}{txt}Missing .: {res}0{txt}/{res}40,885

{txt}{col 19}Mean: {res}{ralign 7:22082.4}{txt} = {res}16jun2020{txt}(+ 11 hours)
{col 14}Std. dev.: {res}{ralign 7:74.9347}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21978}{ralign 11:   22019}{ralign 11:   22084}{ralign 11:   22147}{ralign 11:   22185}
{col 25}{ralign 9:04mar2020}{ralign 11:14apr2020}{ralign 11:18jun2020}{ralign 11:20aug2020}{ralign 11:27sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(203 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(229 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(260 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(270 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(233 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(247 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(244 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(269 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200725
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200725.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200725.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200725.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-25_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-27 20200727
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-27_v4.csv.zip 2020-07-27_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-27_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-27_v4.csv}

{text:successfully unzipped 2020-07-27_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-27_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-27_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,208,768 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,840,765 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(327,096 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22212{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}24oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}310{col 51}{txt}Missing .: {res}0{txt}/{res}40,907

{txt}{col 19}Mean: {res}{ralign 7:22083.6}{txt} = {res}17jun2020{txt}(+ 14 hours)
{col 14}Std. dev.: {res}{ralign 7:75.4198}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21979}{ralign 11:   22020}{ralign 11:   22085}{ralign 11:   22149}{ralign 11:   22187}
{col 25}{ralign 9:05mar2020}{ralign 11:15apr2020}{ralign 11:19jun2020}{ralign 11:22aug2020}{ralign 11:29sep2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(205 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(231 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(262 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(272 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(235 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(247 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(245 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(269 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200727
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200727.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200727.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200727.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-27_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-07-31 20200731
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-07-31_v4.csv.zip 2020-07-31_v4.csv.zip
{txt}
{com}. unzipfile 2020-07-31_v4.csv.zip, replace
{res}{text:    inflating: 2020-07-31_v4.csv}

{text:successfully unzipped 2020-07-31_v4.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-07-31_v4.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-07-31_v4.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 1,947,792 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,623,284 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(288,436 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21903{txt},{res}22216{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20dec2019{txt},{res}28oct2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}314{col 51}{txt}Missing .: {res}0{txt}/{res}36,072

{txt}{col 19}Mean: {res}{ralign 7:22086.1}{txt} = {res}20jun2020{txt}(+ 3 hours)
{col 14}Std. dev.: {res}{ralign 7:76.617}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21979}{ralign 11:   22022}{ralign 11:   22088}{ralign 11:   22152}{ralign 11:   22191}
{col 25}{ralign 9:05mar2020}{ralign 11:17apr2020}{ralign 11:22jun2020}{ralign 11:25aug2020}{ralign 11:03oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(209 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(235 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(266 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(276 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(240 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(249 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(251 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(272 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(0 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200731
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200731.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200731.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200731.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-07-31_v4.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-08 20200808
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-08_v5.csv.zip 2020-08-08_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-08_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-08_v5.csv}

{text:successfully unzipped 2020-08-08_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-08_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-08_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,314,806 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(1,928,978 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(342,940 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21906{txt},{res}22224{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}23dec2019{txt},{res}05nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}319{col 51}{txt}Missing .: {res}0{txt}/{res}42,888

{txt}{col 19}Mean: {res}{ralign 7:22092.2}{txt} = {res}26jun2020{txt}(+ 6 hours)
{col 14}Std. dev.: {res}{ralign 7:77.6496}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21984}{ralign 11:   22027}{ralign 11:   22094}{ralign 11:   22159}{ralign 11:   22198}
{col 25}{ralign 9:10mar2020}{ralign 11:22apr2020}{ralign 11:28jun2020}{ralign 11:01sep2020}{ralign 11:10oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(217 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(243 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(273 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(284 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(248 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(256 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(260 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(280 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(152 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200808
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200808.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200808.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200808.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-08_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-10 20200810
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-10_v5.csv.zip 2020-08-10_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-10_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-10_v5.csv}

{text:successfully unzipped 2020-08-10_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-10_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-10_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,410,980 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,009,120 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(358,722 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21906{txt},{res}22226{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}23dec2019{txt},{res}07nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}321{col 51}{txt}Missing .: {res}0{txt}/{res}43,138

{txt}{col 19}Mean: {res}{ralign 7:22093.5}{txt} = {res}27jun2020{txt}(+ 12 hours)
{col 14}Std. dev.: {res}{ralign 7:78.0566}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21985}{ralign 11:   22028}{ralign 11:   22095}{ralign 11:   22161}{ralign 11:   22200}
{col 25}{ralign 9:11mar2020}{ralign 11:23apr2020}{ralign 11:29jun2020}{ralign 11:03sep2020}{ralign 11:12oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(219 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(245 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(276 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(286 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(250 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(256 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(265 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(282 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(154 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200810
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200810.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200810.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200810.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-10_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-11 20200811
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-11_v5.csv.zip 2020-08-11_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-11_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-11_v5.csv}

{text:successfully unzipped 2020-08-11_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-11_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-11_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,421,690 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,018,200 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(360,189 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21907{txt},{res}22227{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24dec2019{txt},{res}08nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}321{col 51}{txt}Missing .: {res}0{txt}/{res}43,301

{txt}{col 19}Mean: {res}{ralign 7:22094}{txt} = {res}28jun2020{txt}(+ 0 hours)
{col 14}Std. dev.: {res}{ralign 7:78.3414}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21985}{ralign 11:   22028}{ralign 11:   22095}{ralign 11:   22161}{ralign 11:   22201}
{col 25}{ralign 9:11mar2020}{ralign 11:23apr2020}{ralign 11:29jun2020}{ralign 11:03sep2020}{ralign 11:13oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(220 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(246 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(277 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(287 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(251 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(256 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(261 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(284 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(155 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200811
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200811.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200811.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200811.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-11_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-13 20200813
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-13_v5.csv.zip 2020-08-13_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-13_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-13_v5.csv}

{text:successfully unzipped 2020-08-13_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-13_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-13_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,425,170 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,020,950 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(360,864 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21910{txt},{res}22229{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27dec2019{txt},{res}10nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}320{col 51}{txt}Missing .: {res}0{txt}/{res}43,356

{txt}{col 19}Mean: {res}{ralign 7:22095}{txt} = {res}29jun2020{txt}(+ 0 hours)
{col 14}Std. dev.: {res}{ralign 7:78.9193}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21985}{ralign 11:   22028}{ralign 11:   22097}{ralign 11:   22163}{ralign 11:   22203}
{col 25}{ralign 9:11mar2020}{ralign 11:23apr2020}{ralign 11:01jul2020}{ralign 11:05sep2020}{ralign 11:15oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(222 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(248 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(279 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(289 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(253 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(258 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(265 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(287 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(157 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200813
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200813.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200813.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200813.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-13_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-14 20200814
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-14_v5.csv.zip 2020-08-14_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-14_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-14_v5.csv}

{text:successfully unzipped 2020-08-14_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-14_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-14_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,446,680 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,039,090 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(363,879 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21908{txt},{res}22230{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}25dec2019{txt},{res}11nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}323{col 51}{txt}Missing .: {res}0{txt}/{res}43,711

{txt}{col 19}Mean: {res}{ralign 7:22095.8}{txt} = {res}29jun2020{txt}(+ 19 hours)
{col 14}Std. dev.: {res}{ralign 7:79.0204}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21986}{ralign 11:   22029}{ralign 11:   22097}{ralign 11:   22164}{ralign 11:   22204}
{col 25}{ralign 9:12mar2020}{ralign 11:24apr2020}{ralign 11:01jul2020}{ralign 11:06sep2020}{ralign 11:16oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(223 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(249 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(280 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(288 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(254 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(259 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(268 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(287 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(158 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200814
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200814.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200814.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200814.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-14_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-17 20200817
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-17_v5.csv.zip 2020-08-17_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-17_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-17_v5.csv}

{text:successfully unzipped 2020-08-17_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-17_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-17_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,473,770 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,061,410 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(368,172 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21904{txt},{res}22233{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}21dec2019{txt},{res}14nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}330{col 51}{txt}Missing .: {res}0{txt}/{res}44,188

{txt}{col 19}Mean: {res}{ralign 7:22097.3}{txt} = {res}01jul2020{txt}(+ 8 hours)
{col 14}Std. dev.: {res}{ralign 7:79.8429}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21986}{ralign 11:   22030}{ralign 11:   22099}{ralign 11:   22166}{ralign 11:   22207}
{col 25}{ralign 9:12mar2020}{ralign 11:25apr2020}{ralign 11:03jul2020}{ralign 11:08sep2020}{ralign 11:19oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(226 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(252 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(283 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(290 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(257 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(261 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(268 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(291 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(161 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200817
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200817.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200817.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200817.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-17_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-23 20200823
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-23_v5.csv.zip 2020-08-23_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-23_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-23_v5.csv}

{text:successfully unzipped 2020-08-23_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-23_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-23_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,513,310 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,094,540 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(373,959 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21911{txt},{res}22239{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}28dec2019{txt},{res}20nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}329{col 51}{txt}Missing .: {res}0{txt}/{res}44,811

{txt}{col 19}Mean: {res}{ralign 7:22100.7}{txt} = {res}04jul2020{txt}(+ 16 hours)
{col 14}Std. dev.: {res}{ralign 7:81.3688}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21987}{ralign 11:   22032}{ralign 11:   22102}{ralign 11:   22171}{ralign 11:   22212}
{col 25}{ralign 9:13mar2020}{ralign 11:27apr2020}{ralign 11:06jul2020}{ralign 11:13sep2020}{ralign 11:24oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(232 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(258 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(289 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(299 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(263 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(268 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(274 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(295 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(167 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200823
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200823.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200823.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200823.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-23_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-25 20200825
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-25_v5.csv.zip 2020-08-25_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-25_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-25_v5.csv}

{text:successfully unzipped 2020-08-25_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-25_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-25_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,546,310 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,122,010 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(378,918 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21910{txt},{res}22241{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27dec2019{txt},{res}22nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}332{col 51}{txt}Missing .: {res}0{txt}/{res}45,382

{txt}{col 19}Mean: {res}{ralign 7:22101.7}{txt} = {res}05jul2020{txt}(+ 17 hours)
{col 14}Std. dev.: {res}{ralign 7:81.9289}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21988}{ralign 11:   22032}{ralign 11:   22103}{ralign 11:   22172}{ralign 11:   22214}
{col 25}{ralign 9:14mar2020}{ralign 11:27apr2020}{ralign 11:07jul2020}{ralign 11:14sep2020}{ralign 11:26oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(234 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(260 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(291 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(301 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(265 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(270 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(276 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(298 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(169 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200825
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200825.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200825.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200825.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-25_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-27 20200827
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-27_v5.csv.zip 2020-08-27_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-27_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-27_v5.csv}

{text:successfully unzipped 2020-08-27_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-27_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-27_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,564,640 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,137,190 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(381,753 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21904{txt},{res}22243{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}21dec2019{txt},{res}24nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}340{col 51}{txt}Missing .: {res}0{txt}/{res}45,697

{txt}{col 19}Mean: {res}{ralign 7:22102.8}{txt} = {res}06jul2020{txt}(+ 19 hours)
{col 14}Std. dev.: {res}{ralign 7:82.469}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21988}{ralign 11:   22033}{ralign 11:   22104}{ralign 11:   22174}{ralign 11:   22216}
{col 25}{ralign 9:14mar2020}{ralign 11:28apr2020}{ralign 11:08jul2020}{ralign 11:16sep2020}{ralign 11:28oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(236 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(262 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(293 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(303 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(267 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(272 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(282 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(300 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(171 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200827
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200827.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200827.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200827.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-27_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-08-31 20200831
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-08-31_v5.csv.zip 2020-08-31_v5.csv.zip
{txt}
{com}. unzipfile 2020-08-31_v5.csv.zip, replace
{res}{text:    inflating: 2020-08-31_v5.csv}

{text:successfully unzipped 2020-08-31_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-08-31_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-08-31_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,601,780 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,168,090 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(387,369 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21910{txt},{res}22247{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27dec2019{txt},{res}28nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}338{col 51}{txt}Missing .: {res}0{txt}/{res}46,321

{txt}{col 19}Mean: {res}{ralign 7:22104.8}{txt} = {res}08jul2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:83.6009}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21989}{ralign 11:   22034}{ralign 11:   22106}{ralign 11:   22177}{ralign 11:   22219}
{col 25}{ralign 9:15mar2020}{ralign 11:29apr2020}{ralign 11:10jul2020}{ralign 11:19sep2020}{ralign 11:31oct2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(240 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(266 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(297 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(305 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(271 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(275 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(285 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(304 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(175 real changes made)

{com}. 
.  
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200831
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200831.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200831.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200831.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-08-31_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-02 20200902
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-02_v5.csv.zip 2020-09-02_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-02_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-02_v5.csv}

{text:successfully unzipped 2020-09-02_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-02_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-02_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,619,000 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,182,410 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(389,979 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21911{txt},{res}22249{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}28dec2019{txt},{res}30nov2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}339{col 51}{txt}Missing .: {res}0{txt}/{res}46,611

{txt}{col 19}Mean: {res}{ralign 7:22106}{txt} = {res}09jul2020{txt}(+ 23 hours)
{col 14}Std. dev.: {res}{ralign 7:84.0957}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21989}{ralign 11:   22035}{ralign 11:   22107}{ralign 11:   22178}{ralign 11:   22221}
{col 25}{ralign 9:15mar2020}{ralign 11:30apr2020}{ralign 11:11jul2020}{ralign 11:20sep2020}{ralign 11:02nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(242 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(268 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(298 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(309 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(273 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(277 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(279 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(305 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(177 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200902
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200902.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200902.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200902.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-02_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-06 20200906
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-06_v5.csv.zip 2020-09-06_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-06_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-06_v5.csv}

{text:successfully unzipped 2020-09-06_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-06_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-06_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,656,530 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,213,810 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(395,496 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21910{txt},{res}22253{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27dec2019{txt},{res}04dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}344{col 51}{txt}Missing .: {res}0{txt}/{res}47,224

{txt}{col 19}Mean: {res}{ralign 7:22108.1}{txt} = {res}12jul2020{txt}(+ 3 hours)
{col 14}Std. dev.: {res}{ralign 7:85.1186}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21990}{ralign 11:   22036}{ralign 11:   22110}{ralign 11:   22182}{ralign 11:   22225}
{col 25}{ralign 9:16mar2020}{ralign 11:01may2020}{ralign 11:14jul2020}{ralign 11:24sep2020}{ralign 11:06nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(246 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(272 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(303 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(310 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(276 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(281 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(287 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(310 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(181 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200906
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200906.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200906.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200906.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-06_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-08 20200908
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-08_v5.csv.zip 2020-09-08_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-08_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-08_v5.csv}

{text:successfully unzipped 2020-09-08_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-08_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-08_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,675,100 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,229,220 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(398,340 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21910{txt},{res}22255{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27dec2019{txt},{res}06dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}346{col 51}{txt}Missing .: {res}0{txt}/{res}47,540

{txt}{col 19}Mean: {res}{ralign 7:22109.2}{txt} = {res}13jul2020{txt}(+ 4 hours)
{col 14}Std. dev.: {res}{ralign 7:85.71}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21990}{ralign 11:   22037}{ralign 11:   22111}{ralign 11:   22183}{ralign 11:   22227}
{col 25}{ralign 9:16mar2020}{ralign 11:02may2020}{ralign 11:15jul2020}{ralign 11:25sep2020}{ralign 11:08nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(248 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(274 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(305 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(312 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(279 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(283 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(291 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(311 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(183 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200908
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200908.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200908.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200908.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-08_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-12 20200912
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-12_v5.csv.zip 2020-09-12_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-12_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-12_v5.csv}

{text:successfully unzipped 2020-09-12_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-12_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-12_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 2,709,570 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,258,040 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(403,425 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21910{txt},{res}22259{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27dec2019{txt},{res}10dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}350{col 51}{txt}Missing .: {res}0{txt}/{res}48,105

{txt}{col 19}Mean: {res}{ralign 7:22111.4}{txt} = {res}15jul2020{txt}(+ 10 hours)
{col 14}Std. dev.: {res}{ralign 7:86.723}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21991}{ralign 11:   22038}{ralign 11:   22113}{ralign 11:   22186}{ralign 11:   22230}
{col 25}{ralign 9:17mar2020}{ralign 11:03may2020}{ralign 11:17jul2020}{ralign 11:28sep2020}{ralign 11:11nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(251 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(278 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(300 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(315 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(283 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(287 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(294 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(316 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(187 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200912
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200912.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200912.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200912.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-12_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-15 20200915
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-15_v5.csv.zip 2020-09-15_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-15_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-15_v5.csv}

{text:successfully unzipped 2020-09-15_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-15_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-15_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,030,513 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,525,323 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(456,580 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21906{txt},{res}22262{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}23dec2019{txt},{res}13dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}357{col 51}{txt}Missing .: {res}0{txt}/{res}48,610

{txt}{col 19}Mean: {res}{ralign 7:22112.9}{txt} = {res}16jul2020{txt}(+ 22 hours)
{col 14}Std. dev.: {res}{ralign 7:87.5433}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21991}{ralign 11:   22039}{ralign 11:   22114}{ralign 11:   22188}{ralign 11:   22233}
{col 25}{ralign 9:17mar2020}{ralign 11:04may2020}{ralign 11:18jul2020}{ralign 11:30sep2020}{ralign 11:14nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(254 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(281 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(303 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(322 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(285 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(290 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(299 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(319 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(190 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200915
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200915.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200915.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200915.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-15_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-19 20200919
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-19_v5.csv.zip 2020-09-19_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-19_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-19_v5.csv}

{text:successfully unzipped 2020-09-19_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-19_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-19_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,076,515 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,563,768 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(463,450 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21911{txt},{res}22266{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}28dec2019{txt},{res}17dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}356{col 51}{txt}Missing .: {res}0{txt}/{res}49,297

{txt}{col 19}Mean: {res}{ralign 7:22114.8}{txt} = {res}18jul2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:88.7457}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21991}{ralign 11:   22040}{ralign 11:   22116}{ralign 11:   22191}{ralign 11:   22236}
{col 25}{ralign 9:17mar2020}{ralign 11:05may2020}{ralign 11:20jul2020}{ralign 11:03oct2020}{ralign 11:17nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(259 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(285 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(308 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(326 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(290 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(294 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(309 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(322 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(194 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200919
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200919.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200919.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200919.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-19_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-23 20200923
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-23_v5.csv.zip 2020-09-23_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-23_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-23_v5.csv}

{text:successfully unzipped 2020-09-23_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-23_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-23_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,135,759 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,613,006 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(472,530 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21917{txt},{res}22270{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}03jan2020{txt},{res}21dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}354{col 51}{txt}Missing .: {res}0{txt}/{res}50,223

{txt}{col 19}Mean: {res}{ralign 7:22116.7}{txt} = {res}20jul2020{txt}(+ 17 hours)
{col 14}Std. dev.: {res}{ralign 7:90.1001}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21991}{ralign 11:   22040}{ralign 11:   22118}{ralign 11:   22194}{ralign 11:   22240}
{col 25}{ralign 9:17mar2020}{ralign 11:05may2020}{ralign 11:22jul2020}{ralign 11:06oct2020}{ralign 11:21nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(263 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(289 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(309 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(330 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(293 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(303 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(316 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(326 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(198 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200923
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200923.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200923.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200923.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-23_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-09-25 20200925
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-09-25_v5.csv.zip 2020-09-25_v5.csv.zip
{txt}
{com}. unzipfile 2020-09-25_v5.csv.zip, replace
{res}{text:    inflating: 2020-09-25_v5.csv}

{text:successfully unzipped 2020-09-25_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-09-25_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-09-25_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,153,315 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,627,867 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(474,980 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21917{txt},{res}22272{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}03jan2020{txt},{res}23dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}356{col 51}{txt}Missing .: {res}0{txt}/{res}50,468

{txt}{col 19}Mean: {res}{ralign 7:22118}{txt} = {res}22jul2020{txt}(+ 1 hour)
{col 14}Std. dev.: {res}{ralign 7:90.4827}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21992}{ralign 11:   22041}{ralign 11:   22120}{ralign 11:   22196}{ralign 11:   22242}
{col 25}{ralign 9:18mar2020}{ralign 11:06may2020}{ralign 11:24jul2020}{ralign 11:08oct2020}{ralign 11:23nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(254 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(291 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(313 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(327 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(296 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(302 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(318 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(329 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(200 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20200925
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20200925.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20200925.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20200925.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-09-25_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-10-03 20201003
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-10-03_v5.csv.zip 2020-10-03_v5.csv.zip
{txt}
{com}. unzipfile 2020-10-03_v5.csv.zip, replace
{res}{text:    inflating: 2020-10-03_v5.csv}

{text:successfully unzipped 2020-10-03_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-10-03_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-10-03_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,236,112 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,696,683 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(487,690 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21918{txt},{res}22280{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}04jan2020{txt},{res}31dec2020{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}363{col 51}{txt}Missing .: {res}0{txt}/{res}51,739

{txt}{col 19}Mean: {res}{ralign 7:22122.2}{txt} = {res}26jul2020{txt}(+ 5 hours)
{col 14}Std. dev.: {res}{ralign 7:92.6538}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21993}{ralign 11:   22044}{ralign 11:   22124}{ralign 11:   22202}{ralign 11:   22249}
{col 25}{ralign 9:19mar2020}{ralign 11:09may2020}{ralign 11:28jul2020}{ralign 11:14oct2020}{ralign 11:30nov2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(260 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(299 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(311 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(334 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(304 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(310 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(324 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(337 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(208 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201003
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201003.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201003.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201003.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-10-03_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-10-06 20201006
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-10-06_v5.csv.zip 2020-10-06_v5.csv.zip
{txt}
{com}. unzipfile 2020-10-06_v5.csv.zip, replace
{res}{text:    inflating: 2020-10-06_v5.csv}

{text:successfully unzipped 2020-10-06_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-10-06_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-10-06_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,249,171 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,707,560 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(489,690 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21916{txt},{res}22283{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}02jan2020{txt},{res}03jan2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}368{col 51}{txt}Missing .: {res}0{txt}/{res}51,921

{txt}{col 19}Mean: {res}{ralign 7:22123.7}{txt} = {res}27jul2020{txt}(+ 16 hours)
{col 14}Std. dev.: {res}{ralign 7:93.5534}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21993}{ralign 11:   22044}{ralign 11:   22125}{ralign 11:   22204}{ralign 11:   22252}
{col 25}{ralign 9:19mar2020}{ralign 11:09may2020}{ralign 11:29jul2020}{ralign 11:16oct2020}{ralign 11:03dec2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(264 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(302 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(311 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(336 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(307 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(313 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(330 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(339 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(211 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201006
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201006.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201006.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201006.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-10-06_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-10-12 20201012
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-10-12_v5.csv.zip 2020-10-12_v5.csv.zip
{txt}
{com}. unzipfile 2020-10-12_v5.csv.zip, replace
{res}{text:    inflating: 2020-10-12_v5.csv}

{text:successfully unzipped 2020-10-12_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-10-12_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-10-12_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,309,990 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,758,347 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(498,810 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21915{txt},{res}22289{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}01jan2020{txt},{res}09jan2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}375{col 51}{txt}Missing .: {res}0{txt}/{res}52,833

{txt}{col 19}Mean: {res}{ralign 7:22126.9}{txt} = {res}30jul2020{txt}(+ 21 hours)
{col 14}Std. dev.: {res}{ralign 7:95.166}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21994}{ralign 11:   22046}{ralign 11:   22128}{ralign 11:   22209}{ralign 11:   22257}
{col 25}{ralign 9:20mar2020}{ralign 11:11may2020}{ralign 11:01aug2020}{ralign 11:21oct2020}{ralign 11:08dec2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(270 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(308 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(320 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(342 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(313 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(320 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(340 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(347 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(217 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201012
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201012.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201012.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201012.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-10-12_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-10-19 20201019
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-10-19_v5.csv.zip 2020-10-19_v5.csv.zip
{txt}
{com}. unzipfile 2020-10-19_v5.csv.zip, replace
{res}{text:    inflating: 2020-10-19_v5.csv}

{text:successfully unzipped 2020-10-19_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-10-19_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-10-19_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(13 vars, 3,409,197 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(2,840,959 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(513,880 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21916{txt},{res}22296{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}02jan2020{txt},{res}16jan2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}381{col 51}{txt}Missing .: {res}0{txt}/{res}54,358

{txt}{col 19}Mean: {res}{ralign 7:22130.4}{txt} = {res}03aug2020{txt}(+ 9 hours)
{col 14}Std. dev.: {res}{ralign 7:97.1479}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21995}{ralign 11:   22048}{ralign 11:   22132}{ralign 11:   22214}{ralign 11:   22264}
{col 25}{ralign 9:21mar2020}{ralign 11:13may2020}{ralign 11:05aug2020}{ralign 11:26oct2020}{ralign 11:15dec2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(278 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(315 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(325 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(346 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(320 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(326 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(346 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(352 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(224 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201019
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201019.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201019.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201019.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-10-19_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-10-25 20201025
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-10-25_v5.csv.zip 2020-10-25_v5.csv.zip
{txt}
{com}. unzipfile 2020-10-25_v5.csv.zip, replace
{res}{text:    inflating: 2020-10-25_v5.csv}

{text:successfully unzipped 2020-10-25_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-10-25_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-10-25_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 3,831,267 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,192,662 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(580,550 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21879{txt},{res}22302{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}26nov2019{txt},{res}22jan2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}424{col 51}{txt}Missing .: {res}0{txt}/{res}58,055

{txt}{col 19}Mean: {res}{ralign 7:22138.1}{txt} = {res}11aug2020{txt}(+ 2 hours)
{col 14}Std. dev.: {res}{ralign 7:99.8282}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21997}{ralign 11:   22054}{ralign 11:   22142}{ralign 11:   22226}{ralign 11:   22272}
{col 25}{ralign 9:23mar2020}{ralign 11:19may2020}{ralign 11:15aug2020}{ralign 11:07nov2020}{ralign 11:23dec2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(285 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(321 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(334 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(355 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(326 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(332 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(355 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(359 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(230 real changes made)

{com}. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201025
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201025.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201025.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201025.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-10-25_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-10-28 20201028
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-10-28_v5.csv.zip 2020-10-28_v5.csv.zip
{txt}
{com}. unzipfile 2020-10-28_v5.csv.zip, replace
{res}{text:    inflating: 2020-10-28_v5.csv}

{text:successfully unzipped 2020-10-28_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-10-28_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-10-28_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 3,863,055 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,219,163 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(585,354 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21880{txt},{res}22305{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27nov2019{txt},{res}25jan2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}426{col 51}{txt}Missing .: {res}0{txt}/{res}58,538

{txt}{col 19}Mean: {res}{ralign 7:22139.6}{txt} = {res}12aug2020{txt}(+ 15 hours)
{col 14}Std. dev.: {res}{ralign 7:100.668}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21997}{ralign 11:   22055}{ralign 11:   22143}{ralign 11:   22228}{ralign 11:   22275}
{col 25}{ralign 9:23mar2020}{ralign 11:20may2020}{ralign 11:16aug2020}{ralign 11:09nov2020}{ralign 11:26dec2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(287 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(324 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(341 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(359 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(329 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(335 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(346 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(361 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(233 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201028
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201028.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201028.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201028.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-10-28_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-11-01 20201101
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-11-01_v5.csv.zip 2020-11-01_v5.csv.zip
{txt}
{com}. unzipfile 2020-11-01_v5.csv.zip, replace
{res}{text:    inflating: 2020-11-01_v5.csv}

{text:successfully unzipped 2020-11-01_v5.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-11-01_v5.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-11-01_v5.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 3,883,737 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,236,453 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(588,440 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21880{txt},{res}22309{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}27nov2019{txt},{res}29jan2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}430{col 51}{txt}Missing .: {res}0{txt}/{res}58,844

{txt}{col 19}Mean: {res}{ralign 7:22141.8}{txt} = {res}14aug2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:101.739}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21998}{ralign 11:   22056}{ralign 11:   22146}{ralign 11:   22231}{ralign 11:   22278}
{col 25}{ralign 9:24mar2020}{ralign 11:21may2020}{ralign 11:19aug2020}{ralign 11:12nov2020}{ralign 11:29dec2020}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(292 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(327 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(339 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(361 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(333 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(338 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(350 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(365 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(237 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201101
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201101.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201101.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201101.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-11-01_v5.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-11-10 20201110
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-11-10_v6.csv.zip 2020-11-10_v6.csv.zip
{txt}
{com}. unzipfile 2020-11-10_v6.csv.zip, replace
{res}{text:    inflating: 2020-11-10_v6.csv}

{text:successfully unzipped 2020-11-10_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-11-10_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-11-10_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,045,668 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,371,401 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(612,970 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21871{txt},{res}22318{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}18nov2019{txt},{res}07feb2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}448{col 51}{txt}Missing .: {res}0{txt}/{res}61,297

{txt}{col 19}Mean: {res}{ralign 7:22146.8}{txt} = {res}19aug2020{txt}(+ 18 hours)
{col 14}Std. dev.: {res}{ralign 7:104.42}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:21999}{ralign 11:   22059}{ralign 11:   22151}{ralign 11:   22239}{ralign 11:   22287}
{col 25}{ralign 9:25mar2020}{ralign 11:24may2020}{ralign 11:24aug2020}{ralign 11:20nov2020}{ralign 11:07jan2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(302 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(337 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(350 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(369 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(341 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(347 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(360 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(374 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(246 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201110
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201110.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201110.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201110.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-11-10_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-11-14 20201114
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-11-14_v6.csv.zip 2020-11-14_v6.csv.zip
{txt}
{com}. unzipfile 2020-11-14_v6.csv.zip, replace
{res}{text:    inflating: 2020-11-14_v6.csv}

{text:successfully unzipped 2020-11-14_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-11-14_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-11-14_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,095,201 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,412,574 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(620,570 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21871{txt},{res}22322{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}18nov2019{txt},{res}11feb2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}452{col 51}{txt}Missing .: {res}0{txt}/{res}62,057

{txt}{col 19}Mean: {res}{ralign 7:22148.8}{txt} = {res}21aug2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:105.543}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22000}{ralign 11:   22060}{ralign 11:   22153}{ralign 11:   22242}{ralign 11:   22290}
{col 25}{ralign 9:26mar2020}{ralign 11:25may2020}{ralign 11:26aug2020}{ralign 11:23nov2020}{ralign 11:10jan2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(304 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(341 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(354 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(374 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(344 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(353 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(365 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(148 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(379 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(250 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201114
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201114.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201114.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201114.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-11-14_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-11-18 20201118
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-11-18_v6.csv.zip 2020-11-18_v6.csv.zip
{txt}
{com}. unzipfile 2020-11-18_v6.csv.zip, replace
{res}{text:    inflating: 2020-11-18_v6.csv}

{text:successfully unzipped 2020-11-18_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-11-18_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-11-18_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,128,630 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,440,613 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(625,470 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21873{txt},{res}22326{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}20nov2019{txt},{res}15feb2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}454{col 51}{txt}Missing .: {res}0{txt}/{res}62,547

{txt}{col 19}Mean: {res}{ralign 7:22151.2}{txt} = {res}24aug2020{txt}(+ 5 hours)
{col 14}Std. dev.: {res}{ralign 7:106.646}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22000}{ralign 11:   22061}{ralign 11:   22155}{ralign 11:   22245}{ralign 11:   22294}
{col 25}{ralign 9:26mar2020}{ralign 11:26may2020}{ralign 11:28aug2020}{ralign 11:26nov2020}{ralign 11:14jan2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(308 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(345 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(359 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(376 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(347 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(357 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(369 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(152 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(383 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(254 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201118
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201118.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201118.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201118.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-11-18_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-11-23 20201123
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-11-23_v6.csv.zip 2020-11-23_v6.csv.zip
{txt}
{com}. unzipfile 2020-11-23_v6.csv.zip, replace
{res}{text:    inflating: 2020-11-23_v6.csv}

{text:successfully unzipped 2020-11-23_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-11-23_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-11-23_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,184,202 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,486,835 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(633,970 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21876{txt},{res}22331{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}23nov2019{txt},{res}20feb2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}456{col 51}{txt}Missing .: {res}0{txt}/{res}63,397

{txt}{col 19}Mean: {res}{ralign 7:22153.8}{txt} = {res}26aug2020{txt}(+ 20 hours)
{col 14}Std. dev.: {res}{ralign 7:108.014}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22001}{ralign 11:   22063}{ralign 11:   22158}{ralign 11:   22249}{ralign 11:   22298}
{col 25}{ralign 9:27mar2020}{ralign 11:28may2020}{ralign 11:31aug2020}{ralign 11:30nov2020}{ralign 11:18jan2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(314 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(350 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(363 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(382 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(353 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(0 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(362 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(374 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(157 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(389 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(259 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201123
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201123.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201123.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201123.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-11-23_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-11-29 20201129
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-11-29_v6.csv.zip 2020-11-29_v6.csv.zip
{txt}
{com}. unzipfile 2020-11-29_v6.csv.zip, replace
{res}{text:    inflating: 2020-11-29_v6.csv}

{text:successfully unzipped 2020-11-29_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-11-29_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-11-29_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,281,684 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,568,059 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(648,750 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21877{txt},{res}22337{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24nov2019{txt},{res}26feb2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}461{col 51}{txt}Missing .: {res}0{txt}/{res}64,875

{txt}{col 19}Mean: {res}{ralign 7:22156.6}{txt} = {res}29aug2020{txt}(+ 15 hours)
{col 14}Std. dev.: {res}{ralign 7:109.853}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22001}{ralign 11:   22064}{ralign 11:   22161}{ralign 11:   22253}{ralign 11:   22304}
{col 25}{ralign 9:27mar2020}{ralign 11:29may2020}{ralign 11:03sep2020}{ralign 11:04dec2020}{ralign 11:24jan2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(320 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(356 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(371 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(389 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(361 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(410 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(369 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(380 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(163 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(395 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(265 real changes made)

{com}. 
.  
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201129
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201129.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201129.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201129.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-11-29_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-12-04 20201204
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-12-04_v6.csv.zip 2020-12-04_v6.csv.zip
{txt}
{com}. unzipfile 2020-12-04_v6.csv.zip, replace
{res}{text:    inflating: 2020-12-04_v6.csv}

{text:successfully unzipped 2020-12-04_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-12-04_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-12-04_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,339,203 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,616,052 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(657,410 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21877{txt},{res}22342{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24nov2019{txt},{res}03mar2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}466{col 51}{txt}Missing .: {res}0{txt}/{res}65,741

{txt}{col 19}Mean: {res}{ralign 7:22159.2}{txt} = {res}01sep2020{txt}(+ 5 hours)
{col 14}Std. dev.: {res}{ralign 7:111.271}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22002}{ralign 11:   22066}{ralign 11:   22163}{ralign 11:   22257}{ralign 11:   22308}
{col 25}{ralign 9:28mar2020}{ralign 11:31may2020}{ralign 11:05sep2020}{ralign 11:08dec2020}{ralign 11:28jan2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(324 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(360 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(373 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(395 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(366 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(418 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(375 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(383 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(167 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(401 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(270 real changes made)

{com}. 
.  
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201204
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201204.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201204.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201204.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-12-04_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-12-12 20201212
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-12-12_v6.csv.zip 2020-12-12_v6.csv.zip
{txt}
{com}. unzipfile 2020-12-12_v6.csv.zip, replace
{res}{text:    inflating: 2020-12-12_v6.csv}

{text:successfully unzipped 2020-12-12_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-12-12_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-12-12_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,428,303 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,690,247 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(670,960 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21877{txt},{res}22350{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}24nov2019{txt},{res}11mar2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}474{col 51}{txt}Missing .: {res}0{txt}/{res}67,096

{txt}{col 19}Mean: {res}{ralign 7:22163.3}{txt} = {res}05sep2020{txt}(+ 7 hours)
{col 14}Std. dev.: {res}{ralign 7:113.567}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22003}{ralign 11:   22068}{ralign 11:   22167}{ralign 11:   22264}{ralign 11:   22316}
{col 25}{ralign 9:29mar2020}{ralign 11:02jun2020}{ralign 11:09sep2020}{ralign 11:15dec2020}{ralign 11:05feb2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(332 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(367 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(383 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(403 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(374 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(427 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(384 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(393 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(147 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(410 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(278 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201212
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201212.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201212.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201212.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-12-12_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-12-20 20201220
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-12-20_v6.csv.zip 2020-12-20_v6.csv.zip
{txt}
{com}. unzipfile 2020-12-20_v6.csv.zip, replace
{res}{text:    inflating: 2020-12-20_v6.csv}

{text:successfully unzipped 2020-12-20_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-12-20_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-12-20_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,497,894 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,748,212 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(681,528 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21885{txt},{res}22358{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}02dec2019{txt},{res}19mar2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}474{col 51}{txt}Missing .: {res}0{txt}/{res}68,154

{txt}{col 19}Mean: {res}{ralign 7:22167.4}{txt} = {res}09sep2020{txt}(+ 10 hours)
{col 14}Std. dev.: {res}{ralign 7:116.092}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22003}{ralign 11:   22070}{ralign 11:   22172}{ralign 11:   22270}{ralign 11:   22323}
{col 25}{ralign 9:29mar2020}{ralign 11:04jun2020}{ralign 11:14sep2020}{ralign 11:21dec2020}{ralign 11:12feb2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(340 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(375 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(391 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(414 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(382 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(436 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(391 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(400 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(167 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(417 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(286 real changes made)

{com}. 
.  
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201220
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201220.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201220.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201220.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-12-20_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. * update 2020-12-26 20201226
. 
. ******************************
. 
. * get csv file
. 
. clear all
{res}{txt}
{com}. 
. copy https://mrcdata.dide.ic.ac.uk/global-lmic-reports/2020-12-26_v6.csv.zip 2020-12-26_v6.csv.zip
{txt}
{com}. unzipfile 2020-12-26_v6.csv.zip, replace
{res}{text:    inflating: 2020-12-26_v6.csv}

{text:successfully unzipped 2020-12-26_v6.csv.zip to current directory}
{text:total processed:  1}
{text:        skipped:  0}
{text:      extracted:  1}
{txt}
{com}. erase 2020-12-26_v6.csv.zip
{txt}
{com}. 
. 
.  
. ******************************
. 
. * import csv file
. 
. import delimited "2020-12-26_v6.csv", clear varnames(1) 
{res}{txt}(encoding automatically selected: ISO-8859-1)
{res}{text}(14 vars, 4,540,932 obs)

{com}. 
. 
. 
. keep if scenario == "Maintain Status Quo"
{txt}(3,784,088 observations deleted)

{com}. 
. drop scenario
{txt}
{com}. 
. keep if compartment == "deaths"
{txt}(688,040 observations deleted)

{com}. 
. drop compartment
{txt}
{com}. 
. drop y_025 y_25 y_median y_75 y_975 // keeps y_mean
{txt}
{com}. 
. 
. 
. 
. * gen date 
. 
. rename date date_original
{res}{txt}
{com}. gen year = substr(date_original,1,4) 
{txt}
{com}. gen month = substr(date_original,6,2) 
{txt}
{com}. gen day = substr(date_original,9,2) 
{txt}
{com}. egen date2 = concat(day month year)
{txt}
{com}. gen date = date(date2, "DMY", 2050)
{txt}
{com}. format date %tdDDMonCCYY
{txt}
{com}. codebook date

{txt}{hline}
{res}date{right:(unlabeled)}
{txt}{hline}

{col 19}Type: Numeric daily date ({res}float{txt})

{col 18}Range: [{res}21883{txt},{res}22364{txt}]{col 55}Units: {res}1
{col 8}{txt}Or equivalently: [{res}30nov2019{txt},{res}25mar2021{txt}]{col 55}Units: {res}days
{col 10}{txt}Unique values: {res}482{col 51}{txt}Missing .: {res}0{txt}/{res}68,804

{txt}{col 19}Mean: {res}{ralign 6:22169}{txt} = {res}11sep2020{txt}(+ 1 hour)
{col 14}Std. dev.: {res}{ralign 6:118.64}
{txt}{col 12}Percentiles: {col 25}{ralign 9:10%}{ralign 11:25%}{ralign 11:50%}{ralign 11:75%}{ralign 11:90%}
{res}{col 25}{ralign 9:22002}{ralign 11:   22068}{ralign 11:   22172}{ralign 11:   22275}{ralign 11:   22329}
{col 25}{ralign 9:28mar2020}{ralign 11:02jun2020}{ralign 11:14sep2020}{ralign 11:26dec2020}{ralign 11:18feb2021}
{txt}
{com}. drop year month day date2 date_original
{txt}
{com}. 
. 
. rename country loc_grand_name 
{res}{txt}
{com}. 
. replace loc_grand_name = "Brunei" if loc_grand_name == "Brunei Darussalam"
{txt}(413 real changes made)

{com}. replace loc_grand_name = "Cape Verde" if loc_grand_name == "Cabo Verde"
{txt}(382 real changes made)

{com}. replace loc_grand_name = "Congo" if loc_grand_name == "Republic of the Congo"
{txt}(397 real changes made)

{com}. replace loc_grand_name = "Congo DR" if loc_grand_name == "Democratic Republic of Congo"
{txt}(417 real changes made)

{com}. replace loc_grand_name = "Guinea Bissau" if loc_grand_name == "Guinea-Bissau"
{txt}(386 real changes made)

{com}. replace loc_grand_name = "Korea North" if loc_grand_name == "North Korea"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Korea South" if loc_grand_name == "South Korea"
{txt}(443 real changes made)

{com}. replace loc_grand_name = "Kyrgyzstan" if loc_grand_name == "Kyrgyz Republic"
{txt}(395 real changes made)

{com}. replace loc_grand_name = "Laos" if loc_grand_name == "Lao PDR"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Palestine" if loc_grand_name == "State of Palestine"
{txt}(407 real changes made)

{com}. replace loc_grand_name = "Saint Lucia" if loc_grand_name == "St. Lucia"
{txt}(170 real changes made)

{com}. replace loc_grand_name = "Saint Vincent and the Grenadines" if loc_grand_name == "St. Vincent and the Grenadines"
{txt}variable {bf}{res}loc_grand_name{sf}{txt} was {bf}{res}str30{sf}{txt} now {bf}{res}str32{sf}
{txt}(90 real changes made)

{com}. replace loc_grand_name = "Timor Leste" if loc_grand_name == "Timor-Leste"
{txt}(90 real changes made)

{com}. replace loc_grand_name = "United States of America" if loc_grand_name == "United States"
{txt}(424 real changes made)

{com}. replace loc_grand_name = "Viet Nam" if loc_grand_name == "Vietnam"
{txt}(293 real changes made)

{com}. 
. 
. 
. drop iso3c report_date version
{txt}
{com}. 
. rename y_mean DayDeaMeRaIMPES02
{res}{txt}
{com}. 
. order loc_grand_name date DayDeaMeRaIMPES02
{txt}
{com}. 
. rename DayDeaMeRaIMPES02 DayDeaMeRaIMPE20201226
{res}{txt}
{com}. 
. qui compress 
{txt}
{com}. 
. save "IMPE 20201226.dta", replace
{txt}{p 0 4 2}
(file {bf}
IMPE 20201226.dta{rm}
not found)
{p_end}
{p 0 4 2}
file {bf}
IMPE 20201226.dta{rm}
saved
{p_end}

{com}. 
. 
. shell rm -r "2020-12-26_v6.csv" 
{txt}

{com}. 
. ******
. 
. 
. 
. 
. 
. 
. *
. 
. view "log CovidLongitudinal IMPE 1.smcl"
{txt}
{com}. 
. log close
      {txt}name:  {res}<unnamed>
       {txt}log:  {res}/Users/farshadpourmalek/Downloads/CovidLongitudinal-main/code/download/IMPE/log CovidLongitudinal IMPE 1.smcl
  {txt}log type:  {res}smcl
 {txt}closed on:  {res}27 Aug 2023, 17:16:17
{txt}{.-}
{smcl}
{txt}{sf}{ul off}